---
id: TKT-032
seq: 32
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:31Z"
updated_at: "2026-03-12T23:35:23Z"
created_by: human:Leo Audibert
write_hash: 88bb7f22168705c7bf8e676d09ca40068ce1a466e1416672fcbfbd3c6c693040
---

# TKT-032: Add semantic config keys to the config model

## Description
Why: semantic mode needs typed config fields before provider, indexing, or CLI work can be added.

Scope:
- Add typed fields for `semantic.enabled`, `semantic.provider`, `semantic.model`, `semantic.hf_home`, `semantic.sentence_transformers_home`, `semantic.uv_cache_dir`, `semantic.lexical_weight`, and `semantic.vector_weight`.
- Make the fields available through the existing config load/save path.

Files to touch:
- Config model and serialization code under `internal/...`.

Non-goals:
- No defaults, env var overrides, provider logic, or CLI behavior changes.

## Acceptance Criteria
- [x] Config structs expose typed semantic fields and parsing code accepts them without ad-hoc maps. : Typed semantic config fields are implemented in the main config model and covered by config tests.
- [x] No semantic commands or provider logic are introduced in this ticket. : No provider execution or semantic CLI behavior was introduced in the initial config-only slice.

## Handoff
**Current state:**
Typed semantic config fields are present in the main config model.

**Decisions made:**
Keep semantic settings inside the main Docket config file instead of splitting them into a separate semantic config file.

**Files touched:**
- internal/ticket/config.go
- internal/ticket/config_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
