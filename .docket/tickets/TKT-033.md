---
id: TKT-033
seq: 33
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:32Z"
updated_at: "2026-03-11T01:28:21Z"
created_by: human:Leo Audibert
---

# TKT-033: Add semantic config defaults

## Description
Why: semantic mode should work with zero user input and predictable local cache locations.

Scope:
- Define defaults for all semantic config fields.
- Set cache defaults under `~/.cache/docket/...`.
- Choose the initial provider and model defaults.

Files to touch:
- Config defaulting helpers and any home-directory expansion utility already used by Docket.

Non-goals:
- No env var handling, subprocess logic, or index creation.

## Acceptance Criteria
- [x] Default semantic values are defined in one place and are applied when config omits them. : Semantic defaults are centralized in defaultSemanticConfig().
- [x] Default cache paths resolve under `~/.cache/docket/...` without requiring user input. : Default cache paths resolve under ~/.cache/docket/... and are asserted in tests.

## Handoff
**Current state:**
Semantic defaults are defined and applied when config omits them.

**Decisions made:**
Default to the `uv` provider, `sentence-transformers/all-MiniLM-L6-v2`, and cache paths under `~/.cache/docket/`.

**Files touched:**
- internal/ticket/config.go
- internal/ticket/config_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
