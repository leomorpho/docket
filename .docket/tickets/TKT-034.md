---
id: TKT-034
seq: 34
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:32Z"
updated_at: "2026-03-12T23:35:23Z"
created_by: human:Leo Audibert
write_hash: bde50b581452cad1cbac769ad69ecad5ae86ef796014a9f047b7e042f13396f5
---

# TKT-034: Add semantic config environment overrides

## Description
Why: users need local-only overrides for model and cache settings without editing tracked config.

Scope:
- Map env vars onto the semantic config fields.
- Define precedence between flags, env vars, config file values, and defaults.

Files to touch:
- Config loader, env parsing helpers, and config tests.

Non-goals:
- No provider execution or semantic CLI commands.

## Acceptance Criteria
- [x] Documented environment variables override semantic config fields with deterministic precedence. : Environment overrides for semantic config fields are applied during LoadConfig with deterministic precedence.
- [x] Config loading behavior is covered by tests for explicit env vars and fallback defaults. : Config tests cover explicit overrides and invalid env parsing.

## Handoff
**Current state:**
Semantic environment overrides are implemented.

**Decisions made:**
Apply env overrides after defaults and config-file loading so local environment wins predictably.

**Files touched:**
- internal/ticket/config.go
- internal/ticket/config_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
