---
id: TKT-036
seq: 36
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:33Z"
updated_at: "2026-03-11T01:28:29Z"
created_by: human:Leo Audibert
write_hash: 2ddd285d6201db6a158eb4808efe48942ed6bd8ef66ce693661b77d6544368d8
---

# TKT-036: Add semantic provider registry

## Description
Why: provider selection should be centralized instead of spread across future commands and indexer code.

Scope:
- Add a registry or factory that resolves a provider by config name.
- Return deterministic errors for unknown provider names.

Files to touch:
- Semantic provider package and config-to-provider wiring.

Non-goals:
- No real `uv` subprocess behavior beyond what is needed to compile.

## Acceptance Criteria
- [x] Provider names resolve through a single registry instead of `switch` statements spread through the codebase. : NewProvider resolves semantic providers centrally and returns a structured unknown-provider error.
- [x] Unknown provider names return a structured error that can be surfaced by future CLI commands. : Provider tests cover unknown provider handling.

## Handoff
**Current state:**
Provider selection is centralized.

**Decisions made:**
Use a factory function instead of scattering provider selection logic across commands and indexers.

**Files touched:**
- internal/semantic/provider.go
- internal/semantic/provider_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
