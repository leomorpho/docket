---
id: TKT-037
seq: 37
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:33Z"
updated_at: "2026-03-11T01:28:32Z"
created_by: human:Leo Audibert
write_hash: 8569281b269d7d3121113ae621ef858435b716a1fc3bc80f07460e0f3671db98
---

# TKT-037: Add uv provider skeleton

## Description
Why: the `uv` provider should exist as a concrete type before the bridge contract and subprocess details are added.

Scope:
- Add the provider struct, constructor, and placeholder status plumbing.
- Capture model name and cache path inputs in the struct.

Files to touch:
- Semantic provider package and provider construction tests.

Non-goals:
- No `uv` invocation, Python startup, or vector output.

## Acceptance Criteria
- [x] A `uv` provider type can be constructed and reports placeholder status without embedding anything yet. : A concrete UVProvider exists with constructor, script path, repo root, and runner dependencies.
- [x] The provider compiles behind the registry and can be replaced with a fake in tests. : The provider is usable behind the registry and replaceable with a fake runner in tests.

## Handoff
**Current state:**
The uv provider skeleton is implemented as a concrete type.

**Decisions made:**
Inject runner and repo-root dependencies so the provider remains testable and does not hardcode execution assumptions.

**Files touched:**
- internal/semantic/uv.go
- internal/semantic/provider_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
