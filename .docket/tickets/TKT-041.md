---
id: TKT-041
seq: 41
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:34Z"
updated_at: "2026-03-12T23:35:23Z"
created_by: human:Leo Audibert
write_hash: 240bf7a69a0b3b6b5b2923679d47fa6dd8513625e8ae430f8081d64cb37f8eae
---

# TKT-041: Add uv invocation wrapper

## Description
Why: subprocess execution should be isolated from indexing and CLI code.

Scope:
- Launch the bridge script through `uv`.
- Handle stdin, stdout, stderr, and argument construction in one place.

Files to touch:
- `uv` provider implementation and any shared subprocess helper.

Non-goals:
- No availability checks or model downloads yet.

## Acceptance Criteria
- [x] Go can invoke the Python bridge through `uv` with controlled arguments, stdin, stdout, and stderr capture. : The uv provider launches the Python bridge with explicit args, stdin payload, stdout capture, and repo directory.
- [x] Invocation logic is isolated so later provider tests can stub subprocess behavior. : Runner abstraction isolates subprocess execution so tests can stub it directly.

## Handoff
**Current state:**
The Go side can invoke the Python bridge through uv in one isolated execution path.

**Decisions made:**
Wrap subprocess execution behind a runner abstraction instead of directly tying provider logic to exec.CommandContext.

**Files touched:**
- internal/semantic/runner.go
- internal/semantic/uv.go
- internal/semantic/provider_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
