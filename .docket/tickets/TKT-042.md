---
id: TKT-042
seq: 42
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:34Z"
updated_at: "2026-03-12T23:35:23Z"
created_by: human:Leo Audibert
write_hash: 7139c913c265b6d6b1937a5ccedaa5dbd152109e1ff4696a26be956b3dcd98ac
---

# TKT-042: Add uv availability check

## Description
Why: `semantic status` and `related --semantic=auto` need a cheap readiness check.

Scope:
- Detect whether `uv` is installed and executable.
- Return a structured status result suitable for CLI output.

Files to touch:
- `uv` provider package and provider status types.

Non-goals:
- No validation of model downloads or sentence-transformers.

## Acceptance Criteria
- [x] Semantic code can determine whether `uv` is executable without starting an embedding job. : UVProvider.Status now checks uv --version through the runner without starting an embedding job.
- [x] Availability status is exposed in a form that future CLI commands can print directly. : Provider tests cover both available and unavailable uv status cases.

## Handoff
**Current state:**
The semantic provider can report whether uv is callable without starting embeddings.

**Decisions made:**
Use `uv --version` as the cheap readiness probe and expose the output via `Status.Details`.

**Files touched:**
- internal/semantic/uv.go
- internal/semantic/provider_test.go

**Remaining work:**
- Surface provider readiness through the semantic CLI in later tickets.

**AC status:**
- Both acceptance criteria completed and covered by tests.
