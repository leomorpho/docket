---
id: TKT-043
seq: 43
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:35Z"
updated_at: "2026-03-10T23:28:52Z"
created_by: human:Leo Audibert
---

# TKT-043: Add provider cache environment wiring

## Description
Why: local package and model caches must live outside the repo and remain deterministic across runs.

Scope:
- Wire `HF_HOME`, `SENTENCE_TRANSFORMERS_HOME`, and `UV_CACHE_DIR` into the `uv` subprocess environment.
- Respect config and env override resolution.

Files to touch:
- `uv` provider, config resolution, and provider tests.

Non-goals:
- No model downloads or status output.

## Acceptance Criteria
- [x] The `uv` subprocess receives effective cache environment variables derived from config or defaults. : The uv subprocess environment now injects HF_HOME, SENTENCE_TRANSFORMERS_HOME, and UV_CACHE_DIR from resolved config.
- [x] Existing user-provided environment values are respected according to the config precedence rules. : Provider tests verify these values override existing process env entries deterministically.

## Handoff
**Current state:**
The uv bridge receives effective cache environment variables from config resolution.

**Decisions made:**
Replace existing env keys instead of appending duplicates so config resolution stays authoritative.

**Files touched:**
- internal/semantic/uv.go
- internal/semantic/provider_test.go

**Remaining work:**
- Show effective cache paths in semantic status later.

**AC status:**
- Both acceptance criteria completed and covered by tests.
