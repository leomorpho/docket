---
id: TKT-044
seq: 44
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:35Z"
updated_at: "2026-03-12T23:35:23Z"
created_by: human:Leo Audibert
write_hash: 580fee31c19b8058f13a1f511d366614bbc68ddddbdc39e000550b4eb6ffbbcf
---

# TKT-044: Add sentence-transformers dependency pinning

## Description
Why: local setup and embedding behavior should be reproducible across machines and over time.

Scope:
- Choose exact Python package pins for the `uv` execution path.
- Keep the pins in one place so later upgrades are reviewable.

Files to touch:
- `uv` provider implementation and any semantic constants describing the Python toolchain.

Non-goals:
- No lockfile-like artifacts or model changes.

## Acceptance Criteria
- [x] The `uv` provider uses explicit Python package and version pins instead of floating latest dependencies. : Pinned uv package requirements are centralized in UVPinnedPackages.
- [x] Pinned dependency values are referenced from one place so later updates are easy to review. : Provider tests verify the exact pinned package list used in bridge invocation.

## Handoff
**Current state:**
The uv execution path uses explicit pinned Python package requirements.

**Decisions made:**
Keep package pins in one exported slice so updates are reviewable and localized.

**Files touched:**
- internal/semantic/provider.go
- internal/semantic/uv.go
- internal/semantic/provider_test.go

**Remaining work:**
- Revisit pin versions if runtime validation later shows a better package set.

**AC status:**
- Both acceptance criteria completed and covered by tests.
