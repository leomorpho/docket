---
id: TKT-045
seq: 45
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:35Z"
updated_at: "2026-03-12T23:35:23Z"
created_by: human:Leo Audibert
write_hash: 8b8b5e667e23adb47938b2a30f0ed20a609bafab969efdcda25b294e3e0edec6
---

# TKT-045: Add model name plumbing

## Description
Why: provider execution and index freshness both depend on a stable model identifier.

Scope:
- Thread the configured model name from config into provider construction and bridge execution.
- Cover the default model and explicit overrides in tests.

Files to touch:
- Config resolution, provider registry, and `uv` provider.

Non-goals:
- No model change detection or freshness warnings.

## Acceptance Criteria
- [x] The configured semantic model name is passed from Go config into the bridge execution path unchanged. : The configured semantic model is threaded through provider construction and the uv bridge args unchanged.
- [x] Tests cover the default model and an explicit override. : Tests cover both the default model path and explicit model values in provider invocation.

## Handoff
**Current state:**
Model selection is wired from config into the provider and bridge execution path.

**Decisions made:**
Pass model identity explicitly on every bridge invocation instead of relying on hidden script defaults.

**Files touched:**
- internal/ticket/config.go
- internal/ticket/config_test.go
- internal/semantic/uv.go
- internal/semantic/provider_test.go

**Remaining work:**
- Add model-change freshness detection in later semantic tickets.

**AC status:**
- Both acceptance criteria completed and covered by tests.
