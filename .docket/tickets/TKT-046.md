---
id: TKT-046
seq: 46
state: done
priority: 7
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:35Z"
updated_at: "2026-03-10T23:28:58Z"
created_by: human:Leo Audibert
write_hash: 0df856bc761a3767d518e7fcca4a6424e42ca4483de029d65e3d45b804dff2a5
---

# TKT-046: Add real embedding call in Python bridge

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-046` before committing. -->

## Description
Why: this is the first ticket that makes semantic mode produce real vectors.

Scope:
- Load sentence-transformers in the bridge script.
- Embed valid input texts in batch and return numeric vectors using the declared output contract.

Files to touch:
- Python bridge script and any provider parsing/tests that must adapt to real output.

Non-goals:
- No vector storage or ranking.

## Acceptance Criteria
- [x] The bridge script loads sentence-transformers and returns numeric embeddings for valid inputs. : The Python bridge now imports sentence-transformers, loads the configured model, and returns numeric vectors.
- [x] A failed model load produces a deterministic error path instead of a partial silent success. : Offline script tests with a fake sentence_transformers module verify both successful embeddings and deterministic model-load errors.

## Handoff
**Current state:**
The bridge performs real embedding calls via sentence-transformers and returns numeric vectors.

**Decisions made:**
Keep model import and load failures inside the structured JSON bridge contract instead of leaking raw tracebacks as the primary surface.

**Files touched:**
- scripts/semantic_embed.py
- internal/semantic/script_test.go
- internal/semantic/provider_test.go

**Remaining work:**
- Add timeout and richer stderr parsing in the next provider-hardening tickets.

**AC status:**
- Both acceptance criteria completed and covered by tests.
