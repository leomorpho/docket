---
id: TKT-047
seq: 47
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:36Z"
updated_at: "2026-03-12T23:35:23Z"
created_by: human:Leo Audibert
write_hash: 8fa7626352508971dfbf9ed32cea476a2013fa933871be5bc8916b9c33501a37
---

# TKT-047: Add provider timeout handling

## Description
Why: first-run model downloads and local Python startup can hang or take much longer than normal indexing work.

Scope:
- Add timeout control around provider subprocess execution.
- Distinguish timeout failures from normal provider errors.

Files to touch:
- `uv` provider, subprocess helpers, and provider tests.

Non-goals:
- No retries or background downloads.

## Acceptance Criteria
- [x] Stalled `uv` or Python embedding work fails within a bounded timeout instead of hanging the CLI. : UV provider execution is now wrapped in bounded timeouts for status and embed calls.
- [x] Timeout failures are distinguishable from normal provider execution errors. : Provider tests verify timeout failures are surfaced as a distinct provider error kind.

## Handoff
**Current state:**
Stalled uv or Python embedding work now fails within bounded timeouts.

**Decisions made:**
Use provider-level timeouts around status and embed subprocess execution instead of leaving timeout policy to each caller.

**Files touched:**
- internal/semantic/uv.go
- internal/semantic/provider.go
- internal/semantic/provider_test.go

**Remaining work:**
- Tune timeout values if real-world model startup shows they should be adjusted.

**AC status:**
- Both acceptance criteria completed and covered by tests.
