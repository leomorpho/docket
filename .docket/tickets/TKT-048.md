---
id: TKT-048
seq: 48
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:36Z"
updated_at: "2026-03-12T23:35:23Z"
created_by: human:Leo Audibert
write_hash: 1519aa26da403b7ced178a92883f9eb7fc65e2a51e1494222b2f530edcfff508
---

# TKT-048: Add provider stderr and error parsing

## Description
Why: semantic CLI commands will be hard to debug if local provider failures are flattened into one generic error.

Scope:
- Parse stderr and bridge output into structured provider errors.
- Distinguish non-zero exit, invalid JSON, and bridge-declared errors.

Files to touch:
- `uv` provider, bridge contract types, and provider tests.

Non-goals:
- No ranking or index persistence changes.

## Acceptance Criteria
- [x] Bridge failures surface useful structured errors instead of raw opaque subprocess failures. : Provider errors now distinguish bridge-declared failures, malformed JSON, generic process errors, and timeouts.
- [x] Malformed JSON, Python exceptions, and non-zero exits are distinguishable in provider results. : Provider tests cover invalid JSON, structured bridge error responses, plain process failures, and timeout classification.

## Handoff
**Current state:**
Bridge failures now surface as structured provider errors instead of opaque subprocess errors.

**Decisions made:**
Use a typed ProviderError surface so later CLI commands can react differently to timeout, invalid JSON, bridge, and process failures.

**Files touched:**
- internal/semantic/provider.go
- internal/semantic/uv.go
- internal/semantic/provider_test.go

**Remaining work:**
- Expose these structured failures through semantic status and related commands in later tickets.

**AC status:**
- Both acceptance criteria completed and covered by tests.
