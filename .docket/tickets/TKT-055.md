---
id: TKT-055
seq: 55
state: done
priority: 7
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:38Z"
updated_at: "2026-03-11T01:01:18Z"
created_by: human:Leo Audibert
write_hash: c74f647b13b667a20b37807987fba1de937504ff341e0229a2a3c598d2205e57
---

# TKT-055: Add semantic metadata store read and write

## Description
Why: incremental indexing depends on durable local metadata.

Scope:
- Implement persistence APIs for semantic metadata.
- Support repeated rebuild runs without duplicate or inconsistent state.

Files to touch:
- Semantic metadata store package and tests.

Non-goals:
- No similarity queries or ranking.

## Acceptance Criteria
- [x] Code can persist and load semantic metadata records without touching committed ticket files. : Metadata load/save plus chunk upsert, lookup, remove, and sorted iteration are implemented and tested.
- [x] Read and write operations are idempotent and safe for repeated rebuild runs. : Metadata read/write paths are idempotent and safe across repeated save/load cycles.

## Handoff
**Current state:**
Semantic metadata can be persisted and reloaded locally without touching committed ticket files.

**Decisions made:**
Provide a small metadata API now so later diff/index code can build on it directly instead of re-reading raw JSON.

**Files touched:**
- internal/semantic/metadata.go
- internal/semantic/metadata_test.go

**Remaining work:**
- Use metadata persistence in diff detection and index rebuild flows later.

**AC status:**
- Both acceptance criteria completed and covered by tests.
