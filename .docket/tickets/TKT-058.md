---
id: TKT-058
seq: 58
state: done
priority: 7
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:39Z"
updated_at: "2026-03-12T23:35:24Z"
created_by: human:Leo Audibert
write_hash: 2cdd0449b54d3b6672401aaf678247d85c280604a2991a5733a255c7745744fd
---

# TKT-058: Add vector upsert operation

## Description
Why: incremental indexing needs a precise write path for changed chunks.

Scope:
- Add vector upsert for one semantic chunk record.
- Store chunk and ticket metadata alongside the vector.

Files to touch:
- Semantic vector-store package and tests.

Non-goals:
- No diffing or similarity queries.

## Acceptance Criteria
- [x] A single semantic chunk embedding can be inserted or updated in the local vector store with metadata. : VectorStore.Upsert inserts semantic chunk embeddings with ticket and chunk metadata, replacing existing IDs via delete-then-add.
- [x] Upserting the same chunk ID twice replaces stale metadata instead of duplicating rows. : Vector tests verify repeated upserts replace stale content and metadata instead of duplicating records.

## Handoff
**Current state:**
Vector documents can be inserted and replaced deterministically by chunk ID.

**Decisions made:**
Implement upsert as delete-then-add because the underlying library exposes add and delete directly without a dedicated update API.

**Files touched:**
- internal/semantic/vector.go
- internal/semantic/vector_test.go

**Remaining work:**
- Drive upserts from incremental diff results later.

**AC status:**
- Both acceptance criteria completed and covered by tests.
