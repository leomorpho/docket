---
id: TKT-059
seq: 59
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:39Z"
updated_at: "2026-03-11T01:04:50Z"
created_by: human:Leo Audibert
---

# TKT-059: Add vector delete operation

## Description
Why: incremental indexing must remove stale embeddings after ticket edits.

Scope:
- Delete stored semantic chunks by deterministic chunk ID.
- Make delete safe when the chunk is already absent.

Files to touch:
- Semantic vector-store package and tests.

Non-goals:
- No full rebuild or diff calculation.

## Acceptance Criteria
- [x] Removed semantic chunks can be deleted from the vector store by deterministic chunk ID. : VectorStore.Delete removes semantic chunks by deterministic ID and remains safe when the ID is already absent.
- [x] Delete operations are safe to call when the chunk is already absent. : Vector tests cover deleting an existing ID and deleting a missing ID without failure.

## Handoff
**Current state:**
Vector records can be removed safely by chunk ID.

**Decisions made:**
Treat missing deletes as a no-op so incremental cleanup stays idempotent.

**Files touched:**
- internal/semantic/vector.go
- internal/semantic/vector_test.go

**Remaining work:**
- Feed deletions from chunk diffing later.

**AC status:**
- Both acceptance criteria completed and covered by tests.
