---
id: TKT-061
seq: 61
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:39Z"
updated_at: "2026-03-12T23:35:24Z"
created_by: human:Leo Audibert
write_hash: 208477e2e7276a192043370bb2b2e364b17e48a3057838c400d6cba5274a09d5
---

# TKT-061: Add semantic index path configuration

## Description
Why: local semantic artifacts should be rebuildable, discoverable, and not hardcoded in multiple packages.

Scope:
- Define canonical paths for semantic metadata and vector storage under `.docket/`.
- Expose them through one helper API.

Files to touch:
- Semantic path helpers and any config plumbing needed for local store paths.

Non-goals:
- No `.gitignore` changes or collection initialization.

## Acceptance Criteria
- [x] Semantic metadata and vector index paths are resolved under `.docket/` through one shared helper. : SemanticDir, MetadataPath, and VectorDBPath now resolve all local semantic artifacts under .docket/semantic.
- [x] Future commands do not hardcode semantic file locations in multiple packages. : Vector and metadata tests cover the canonical semantic path layout.

## Handoff
**Current state:**
Semantic local paths are now centralized under `.docket/semantic`.

**Decisions made:**
Use one shared path helper surface so later commands do not hardcode semantic file locations in multiple places.

**Files touched:**
- internal/semantic/metadata.go
- internal/semantic/vector.go
- internal/semantic/vector_test.go

**Remaining work:**
- Add gitignore and higher-level semantic command use of these paths later.

**AC status:**
- Both acceptance criteria completed and covered by tests.
