---
id: TKT-062
seq: 62
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:40Z"
updated_at: "2026-03-11T01:28:03Z"
created_by: human:Leo Audibert
---

# TKT-062: Add semantic gitignore entries

## Description
Why: semantic mode is intended to be local-only and rebuildable.

Scope:
- Ignore generated semantic artifacts under `.docket/`.
- Cover the actual paths chosen for semantic metadata and vector storage.

Files to touch:
- Root `.gitignore` and any doc that explains local semantic artifacts.

Non-goals:
- No home-directory cache paths in repo ignores.

## Acceptance Criteria
- [x] Repo-local semantic artifacts are explicitly gitignored so vector data and local caches never appear as tracked changes. : Semantic artifacts under .docket/semantic/ are now explicitly gitignored.
- [x] Ignored patterns cover the actual semantic index paths chosen by the implementation. : The ignore rules cover the semantic metadata and vector-store paths introduced by the implementation.

## Handoff
**Current state:**
Repo-local semantic artifacts are now ignored by git.

**Decisions made:**
Ignore the entire `.docket/semantic/` directory because all semantic artifacts inside it are local-only and rebuildable.

**Files touched:**
- .gitignore

**Remaining work:**
- Keep any future semantic local artifacts under the same directory so ignore behavior stays centralized.

**AC status:**
- Both acceptance criteria completed.
