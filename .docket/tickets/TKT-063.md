---
id: TKT-063
seq: 63
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:40Z"
updated_at: "2026-03-11T01:28:05Z"
created_by: human:Leo Audibert
---

# TKT-063: Add full-scan ticket enumerator

## Description
Why: rebuild and incremental indexing need one shared source of tickets to inspect.

Scope:
- Enumerate all tickets eligible for semantic indexing.
- Make inclusion rules for states like `done` and `archived` explicit.

Files to touch:
- Semantic indexing package and tests.

Non-goals:
- No chunk diffing or provider calls.

## Acceptance Criteria
- [x] Semantic indexing can enumerate all tickets eligible for embedding through one reusable function. : EnumerateTickets now provides one reusable ticket enumeration path for semantic indexing.
- [x] The enumerator has explicit rules for archived and done tickets instead of leaving inclusion ambiguous. : The local backend path reads ticket markdown directly and explicitly excludes archived tickets while including done tickets.

## Handoff
**Current state:**
Semantic indexing has a reusable full-scan ticket enumerator.

**Decisions made:**
Use markdown files as the source of truth for the local backend instead of relying on the SQLite cache, and include `done` while excluding `archived`.

**Files touched:**
- internal/semantic/index.go
- internal/semantic/index_test.go

**Remaining work:**
- Reuse the same enumerator in semantic CLI commands later.

**AC status:**
- Both acceptance criteria completed and covered by tests.
