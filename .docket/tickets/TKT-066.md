---
id: TKT-066
seq: 66
state: done
priority: 7
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:52:41Z"
updated_at: "2026-03-12T23:35:24Z"
created_by: human:Leo Audibert
write_hash: 6da763322cc08342f467226ad25fae825dd1be3cde054db84af806200696b716
---

# TKT-066: Add full semantic rebuild path

## Description
Why: users need a clean recovery path when the semantic index becomes inconsistent or the schema changes.

Scope:
- Clear and recreate local semantic state.
- Rebuild from all eligible tickets with consistent metadata and vector state.

Files to touch:
- Semantic indexer package and vector/metadata store integration.

Non-goals:
- No CLI exposure yet.

## Acceptance Criteria
- [x] Docket can wipe or recreate the local semantic index and rebuild it from all eligible tickets. : FullRebuild now clears the local vector DB and metadata state before rebuilding from all eligible tickets.
- [x] A full rebuild leaves metadata and vector state consistent even after previous partial index runs. : Tests verify a full rebuild recreates consistent local state and freshness behavior afterwards.

## Handoff
**Current state:**
A full semantic rebuild path exists and recreates local semantic state from scratch.

**Decisions made:**
Reset vector storage and metadata before delegating to the incremental rebuild path, rather than maintaining a separate indexing code path.

**Files touched:**
- internal/semantic/index.go
- internal/semantic/index_test.go

**Remaining work:**
- Expose full rebuild through CLI in later tickets.

**AC status:**
- Both acceptance criteria completed and covered by tests.
