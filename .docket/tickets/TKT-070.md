---
id: TKT-070
seq: 70
state: validated
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:56:31Z"
updated_at: "2026-03-11T01:32:34Z"
created_by: human:Leo Audibert
write_hash: d76a95e7359467d44b7abc5506b717eb069c33053f27d9573beb1f90de8d08a5
---

# TKT-070: Add provider details to semantic status

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-070` before committing. -->

## Description
Why: users need to see which provider and model semantic mode is attempting to use.

Scope:
- Print provider name, model name, and provider availability in `semantic status`.
- Reuse provider status types instead of ad-hoc strings.

Files to touch:
- Semantic status command and provider status plumbing.

Non-goals:
- No cache-path or index-stat output.

## Acceptance Criteria
- [x] `semantic status` shows provider name, model name, and availability. : Semantic status now prints provider name, configured model, provider availability, and provider details.
- [x] Provider status output uses structured provider data rather than duplicated string assembly. : Command tests validate the provider fields in JSON output.

## Handoff
**Current state:**
Provider details are exposed through `docket semantic status`.

**Decisions made:**
Reuse the provider status object directly rather than duplicating provider formatting logic in the command.

**Files touched:**
- cmd/semantic_status.go
- cmd/semantic_test.go

**Remaining work:**
- Surface provider warnings in other semantic commands later.

**AC status:**
- Both acceptance criteria completed and covered by tests.
