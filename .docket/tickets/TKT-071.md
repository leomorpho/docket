---
id: TKT-071
seq: 71
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:56:31Z"
updated_at: "2026-03-11T01:32:36Z"
created_by: human:Leo Audibert
---

# TKT-071: Add cache paths to semantic status

## Description
Why: local semantic mode should make its effective cache locations transparent.

Scope:
- Print resolved `HF_HOME`, `SENTENCE_TRANSFORMERS_HOME`, and `UV_CACHE_DIR` in `semantic status`.
- Show post-resolution values after config and env precedence is applied.

Files to touch:
- Semantic status command and config resolution helpers.

Non-goals:
- No index statistics.

## Acceptance Criteria
- [x] `semantic status` shows effective cache paths after env, config, and default resolution. : Semantic status prints effective HF_HOME, SENTENCE_TRANSFORMERS_HOME, and UV_CACHE_DIR values from resolved config.
- [x] The printed paths match the exact values passed to provider subprocess execution. : These status values come from the same config resolution path used by provider execution.

## Handoff
**Current state:**
Effective semantic cache paths are now visible in `semantic status`.

**Decisions made:**
Display post-resolution config values so status matches what provider subprocesses actually receive.

**Files touched:**
- cmd/semantic_status.go
- cmd/semantic_shared.go
- cmd/semantic_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
