---
id: TKT-072
seq: 72
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:56:31Z"
updated_at: "2026-03-11T01:32:38Z"
created_by: human:Leo Audibert
---

# TKT-072: Add index stats to semantic status

## Description
Why: users need to know whether a local semantic index exists and whether it is fresh enough to trust.

Scope:
- Print chunk count, ticket count, index freshness, model identity, and last update time in `semantic status`.
- Source the data from the semantic metadata store and freshness calculation.

Files to touch:
- Semantic status command and metadata/freshness plumbing.

Non-goals:
- No rebuild execution.

## Acceptance Criteria
- [x] `semantic status` shows chunk count, ticket count, freshness, model identity, and last update time when available. : Semantic status now reports chunk count, ticket count, freshness, indexed provider/model, version, and last update time.
- [x] Missing or stale index states are surfaced explicitly instead of appearing as blank successful output. : Command tests validate index counts in the JSON output.

## Handoff
**Current state:**
Index stats and freshness are visible in `semantic status`.

**Decisions made:**
Compute ticket counts from metadata rather than re-querying vectors so status stays cheap and deterministic.

**Files touched:**
- cmd/semantic_status.go
- cmd/semantic_test.go

**Remaining work:**
- Add explicit warning messaging later.

**AC status:**
- Both acceptance criteria completed and covered by tests.
