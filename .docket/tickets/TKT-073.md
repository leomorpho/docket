---
id: TKT-073
seq: 73
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:56:31Z"
updated_at: "2026-03-12T23:35:24Z"
created_by: human:Leo Audibert
write_hash: f4ff8fd4256a4fff4c9a548837b68dfbc8316bfac332c110c52792caaf19d998
---

# TKT-073: Add semantic rebuild command shell

## Description
Why: rebuild behavior needs a stable CLI entrypoint before incremental and full execution are wired in.

Scope:
- Add `docket semantic rebuild` command wiring and flags.
- Define output and exit-code behavior for success and failure.

Files to touch:
- CLI command registration and semantic rebuild command package.

Non-goals:
- No actual rebuild work yet.

## Acceptance Criteria
- [x] A `docket semantic rebuild` command exists and is reachable from the CLI help surface. : A  command is registered and reachable from CLI help.
- [x] The command has a stable flag and output contract that later rebuild tickets can implement. : The command has stable human and JSON output contracts around rebuild mode and change counters.

## Handoff
**Current state:**
`docket semantic rebuild` exists and is reachable from the CLI.

**Decisions made:**
Keep the command output small and structured around mode plus add/change/delete/unchanged counts.

**Files touched:**
- cmd/semantic_rebuild.go
- cmd/semantic_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
