---
id: TKT-075
seq: 75
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:56:31Z"
updated_at: "2026-03-11T01:32:45Z"
created_by: human:Leo Audibert
write_hash: aa15f35b2d1ad701e2a77b8631e442a4a27880505432fd2af302c048be7496e0
---

# TKT-075: Add full rebuild flag

## Description
Why: users need an explicit CLI path to force a clean rebuild when local semantic state is stale or corrupted.

Scope:
- Add `--full` to `semantic rebuild`.
- Route it to the full rebuild path and keep default behavior incremental.

Files to touch:
- Semantic rebuild command and full indexer integration.

Non-goals:
- No change to incremental default behavior.

## Acceptance Criteria
- [x] `semantic rebuild --full` forces a complete rebuild instead of the incremental path. : now routes to the full rebuild path while the default command remains incremental.
- [x] Running `semantic rebuild` without `--full` continues to use incremental behavior. : Command tests verify full mode JSON output and mode selection.

## Handoff
**Current state:**
The semantic rebuild command supports both incremental and full modes.

**Decisions made:**
Use a simple `--full` flag rather than separate commands so rebuild semantics stay discoverable in one place.

**Files touched:**
- cmd/semantic_rebuild.go
- cmd/semantic_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
