---
id: TKT-080
seq: 80
state: validated
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:56:31Z"
updated_at: "2026-03-11T01:43:39Z"
created_by: human:Leo Audibert
write_hash: 4e21021cf51f97d1d4a41a5f81879037f1492bad66f898787253e8dff2ddf8d2
---

# TKT-080: Add graph boost input model

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-080` before committing. -->

## Description
Why: graph signals should be additive inputs to ranking, not hidden heuristics.

Scope:
- Define the graph edges that can influence related-ticket ranking, such as ticket links, dependencies, or recent session adjacency.
- Represent those inputs in a typed model.

Files to touch:
- Semantic ranking package and any ticket graph helpers.

Non-goals:
- No score calculation yet.

## Acceptance Criteria
- [x] The graph edges that can influence related-ticket ranking are explicitly defined in code. : Graph edge kinds are explicitly defined in code for dependency, commit, and session-adjacency signals.
- [x] Graph inputs are represented in a typed model that later ranking code can consume without extra parsing. : Graph inputs are represented through typed semantic.GraphInputs and semantic.GraphEdge values.

## Handoff
**Current state:**
Graph scoring inputs are represented through typed graph edge models.

**Decisions made:**
Model graph signals explicitly as GraphInputs and GraphEdge values so related ranking can consume ticket graph data without stringly-typed parsing.

**Files touched:**
- internal/semantic/ranking.go
- internal/semantic/ranking_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
