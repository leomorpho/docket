---
id: TKT-083
seq: 83
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:57:34Z"
updated_at: "2026-03-11T01:43:43Z"
created_by: human:Leo Audibert
write_hash: ecf20d3110bbd9677fa077fb9a6280916ef5e8e22df062f7c3343c1380781b12
---

# TKT-083: Add semantic mode flag handling

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-083` before committing. -->

## Description
Why: `docket related` needs explicit mode selection semantics before execution paths are implemented.

Scope:
- Add `--semantic=off|auto|on` parsing.
- Define fallback and failure behavior for each mode.

Files to touch:
- Related command package and any shared semantic CLI option parsing.

Non-goals:
- No lexical or semantic query execution.

## Acceptance Criteria
- [x] The related command accepts `--semantic=off|auto|on` and rejects invalid values. : The related command accepts --semantic=off|auto|on and rejects invalid values.
- [x] Mode semantics for fallback and hard failure are defined in code before execution logic is added. : Mode semantics are defined in code through semanticReady and semanticUnavailableReason handling.

## Handoff
**Current state:**
`docket related` now accepts explicit semantic mode selection.

**Decisions made:**
Model semantic mode as a typed enum with parse-time validation so fallback and failure behavior are defined before command execution.

**Files touched:**
- cmd/related.go
- cmd/related_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
