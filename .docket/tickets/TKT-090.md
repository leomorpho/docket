---
id: TKT-090
seq: 90
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:57:35Z"
updated_at: "2026-03-11T01:48:47Z"
created_by: human:Leo Audibert
---

# TKT-090: Add uv requirement docs

## Description
Why: semantic setup will fail confusingly if the repo documents the feature but not the local `uv` dependency.

Scope:
- Document the requirement that `uv` be installed for the initial provider.
- Document the failure mode when `uv` is missing.

Files to touch:
- README and semantic setup docs.

Non-goals:
- No provider implementation changes.

## Acceptance Criteria
- [x] Docs state that `uv` must be installed to use the initial semantic provider. : README states that uv must be installed to use the initial semantic provider.
- [x] Docs explain what users should expect from `semantic status` or `related` when `uv` is unavailable. : README explains the warning and fallback behavior when uv is unavailable.

## Handoff
**Current state:**
README now states that uv is required for the initial semantic provider and describes the failure surface.

**Decisions made:**
Document the expected behavior of semantic status and related when uv is unavailable instead of adding more command-specific setup text.

**Files touched:**
- README.md

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed.
