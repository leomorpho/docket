---
id: TKT-096
seq: 96
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:57:36Z"
updated_at: "2026-03-11T01:49:31Z"
created_by: human:Leo Audibert
write_hash: a87abf875b3a78af066b91dcd6f88ed7f22a5ca2bcdfc9f6202559211ecf6a4f
---

# TKT-096: Add semantic rebuild CLI tests

## Description
Why: rebuild behavior needs regression protection around default incremental and explicit full modes.

Scope:
- Add CLI tests for incremental rebuild, no-op rebuild, and `--full` rebuild behavior.
- Verify failure handling for provider or index errors.

Files to touch:
- Semantic rebuild command tests.

Non-goals:
- No new rebuild logic.

## Acceptance Criteria
- [x] CLI tests cover incremental rebuild, no-op rebuild, and `semantic rebuild --full` behavior. : Semantic rebuild CLI tests cover incremental rebuild, no-op rebuild, and full rebuild behavior.
- [x] Tests verify rebuild command failure handling for provider or local-store errors. : Tests verify rebuild command failure handling when the rebuild path returns an error.

## Handoff
**Current state:**
Semantic rebuild tests now cover incremental output, no-op rebuilds, full rebuilds, and failure handling.

**Decisions made:**
Keep rebuild tests command-level and provider-independent by stubbing the rebuild functions directly.

**Files touched:**
- cmd/semantic_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
