---
id: TKT-097
seq: 97
state: done
priority: 8
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:57:36Z"
updated_at: "2026-03-11T01:49:36Z"
created_by: human:Leo Audibert
---

# TKT-097: Add related command CLI tests

## Description
Why: the related command will have multiple execution and fallback modes that are easy to regress.

Scope:
- Add CLI tests for `--semantic=off`, `auto`, and `on`.
- Verify output and failure behavior when provider or index readiness changes.

Files to touch:
- Related command tests.

Non-goals:
- No ranking implementation changes.

## Acceptance Criteria
- [x] CLI tests cover `docket related` with `--semantic=off`, `auto`, and `on`. : Related command CLI tests cover semantic off, auto fallback, and semantic-on failure modes.
- [x] Tests verify lexical fallback and hard-failure behavior for missing semantic readiness. : Tests verify lexical fallback and hard-failure behavior when semantic readiness is missing.

## Handoff
**Current state:**
Related-command tests cover semantic off, auto fallback, and required semantic-on failure behavior.

**Decisions made:**
Use command-level tests with injected provider and vector scoring hooks so fallback semantics stay explicit and fast.

**Files touched:**
- cmd/related_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
