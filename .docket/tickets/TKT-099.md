---
id: TKT-099
seq: 99
state: done
priority: 7
labels:
    - feature
    - llm-only
created_at: "2026-03-10T22:57:36Z"
updated_at: "2026-03-11T01:49:45Z"
created_by: human:Leo Audibert
---

# TKT-099: Add end-to-end semantic indexing test with fake provider

## Description
Why: one end-to-end test should prove that indexing and related-ticket retrieval work together without requiring a real local model.

Scope:
- Index a small ticket fixture set using a fake provider.
- Verify that related-ticket lookup returns the expected neighbors.

Files to touch:
- End-to-end semantic test file and any fake provider fixtures.

Non-goals:
- No real model download or networked provider.

## Acceptance Criteria
- [x] An end-to-end test indexes a small ticket set using a fake provider and writes local semantic state. : An end-to-end test indexes a small ticket fixture set using a fake provider and writes local semantic state.
- [x] The test verifies that related-ticket lookup returns the expected nearest neighbors from the indexed fixture set. : The test verifies that related-ticket lookup returns the expected nearest neighbor from the indexed fixture set.

## Handoff
**Current state:**
An end-to-end command test now rebuilds semantic state with a fake provider and verifies related-ticket retrieval against the real local index.

**Decisions made:**
Implement the end-to-end path at the command layer so semantic rebuild and related retrieval are exercised together.

**Files touched:**
- cmd/related_test.go

**Remaining work:**
- None for this ticket.

**AC status:**
- Both acceptance criteria completed and covered by tests.
