---
id: TKT-107
seq: 107
state: done
priority: 2
created_at: "2026-03-11T18:50:58Z"
updated_at: "2026-03-11T19:17:35Z"
created_by: agent:claude-sonnet-4-6
---

# TKT-107: Add parent/child ticket relationships (epics)

## Description
Add a `parent` field to ticket frontmatter enabling recursive ticket grouping (epics). Children are derived at runtime by scanning the index — no stored `children` field.

## Acceptance Criteria
- [x] Add 'parent' field to Ticket struct and parser; validate it points to an existing ticket on write : Added parent to Ticket struct, parser/writer frontmatter, and write-time parent existence validation in local store
- [x] Cycle detection: reject any parent assignment that would create a cycle : Store write path rejects parent cycles (validateParentRef traverses ancestor chain and errors on loops)
- [x] Depth warning: after any parent assignment, traverse depth and warn if > 3 levels : Update command emits warning when parent assignment depth exceeds 3 via ParentDepth()
- [x] Index builds reverse parent→children map at load time; no stored children field : Added runtime RelationshipIndex with reverse parent→children map built from loaded tickets (no stored children field)
- [x] docket list renders indented tree by depth when parent/child relationships exist : List command now renders tree rows with indentation/↳ based on parent-child depth
- [x] docket show on a parent aggregates AC counts across all descendants : Show command now aggregates descendant AC completion totals for parent tickets
- [x] docket update --state stale errors if open children exist, lists them, and accepts --cascade flag : Update --state stale now blocks when open descendants exist and supports --cascade to apply stale recursively
- [x] Manifest _warning key added; tamper detection runs on every CLI command that loads tickets : Added .docket/manifest.json with _warning and root pre-run tamper detection hook
- [x] help-json includes top-level agent_instructions warning LLMs not to edit .md or manifest files directly : help-json now includes top-level agent_instructions warning against direct markdown/manifest edits
- [x] docket validate output is prescriptive: maps each detected direct-edit field change to the correct CLI command : validate now reports direct-edit field deltas with explicit corrective docket update commands

