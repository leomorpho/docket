---
id: TKT-134
seq: 134
state: done
priority: 2
labels:
    - feature
    - ac-enforcement
    - dx
created_at: "2026-03-12T01:57:55Z"
updated_at: "2026-03-14T02:12:34Z"
created_by: agent:claude-sonnet-4-6
write_hash: 4b108a59f295ca5c228444520be4854ec5ff4dc205522c84d567e87a49eb93d0
---

# TKT-134: Smart defaults by stack — auto-inject AC on ticket creation

## Description
When a ticket is created, detect the project's tech stack and inject default AC items with runnable verification commands. Stack detection reads package.json, pyproject.toml, go.mod, Cargo.toml, etc.

Built-in defaults:
- TypeScript/JS: tsc --noEmit, pnpm test, pnpm test --coverage
- Python: pytest, pytest --cov (>= 80%)
- Go: go test ./...
- Rust: cargo test

User can override/extend in .docket/config.yaml:
  ac_defaults:
    typescript:
      - desc: "No TypeScript errors"
        run: "pnpm tsc --noEmit"
      - desc: "Tests pass with coverage"
        run: "pnpm test --coverage"

Defaults are only injected if no ACs already exist on the ticket. Depends on TKT-133 (runnable AC commands).

## Acceptance Criteria
- [x] docket create in a TypeScript project auto-adds at least 2 runnable AC items : Added stack detection and AC default injection in create via cmd/ac_defaults.go with config.yaml override behavior and --no-ac-defaults, plus tests in cmd/create_test.go and cmd/ac_defaults_test.go.
- [x] Stack is detected from root-level manifest files (package.json, pyproject.toml, go.mod, Cargo.toml, etc.) : Added stack detection and AC default injection in create via cmd/ac_defaults.go with config.yaml override behavior and --no-ac-defaults, plus tests in cmd/create_test.go and cmd/ac_defaults_test.go.
- [x] User config in .docket/config.yaml ac_defaults overrides built-in defaults (not merges) : Added stack detection and AC default injection in create via cmd/ac_defaults.go with config.yaml override behavior and --no-ac-defaults, plus tests in cmd/create_test.go and cmd/ac_defaults_test.go.
- [x] --no-ac-defaults flag on docket create skips stack-default AC injection : Added stack detection and AC default injection in create via cmd/ac_defaults.go with config.yaml override behavior and --no-ac-defaults, plus tests in cmd/create_test.go and cmd/ac_defaults_test.go.
- [x] Unknown stacks silently skip injection — detection is graceful with no errors : Added stack detection and AC default injection in create via cmd/ac_defaults.go with config.yaml override behavior and --no-ac-defaults, plus tests in cmd/create_test.go and cmd/ac_defaults_test.go.

## Handoff
*Last updated: 2026-03-14T02:12:34Z by human:Codex*

**Current state:** done

**Decisions made:** Historical ticket completed before structured handoff became mandatory; existing AC, comments, and linked changes remain the source of detail.

**Files touched:** Refer to linked commits, comments, and repository history for the exact files involved.

**Remaining work:** No additional work recorded at the time this handoff was backfilled.

**AC status:** Completed in the historical ticket record.
