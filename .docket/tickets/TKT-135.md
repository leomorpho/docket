---
id: TKT-135
seq: 135
state: done
priority: 2
labels:
    - feature
    - hooks
    - dx
created_at: "2026-03-12T01:59:04Z"
updated_at: "2026-03-12T23:35:26Z"
created_by: agent:claude-sonnet-4-6
write_hash: 09b652c1feb27dabf3b47b4239e3fa08f8303c662a5041f1a02813007476b38a
---

# TKT-135: Human interactive AC prompt in pre-commit hook

## Description
When a human runs git commit and the commit references a docket ticket, the pre-commit hook renders the AC list interactively. For each incomplete AC item, it asks the user to confirm completion. Runnable ACs (TKT-133) are executed automatically; prose-only ACs get a y/n prompt.

Flow:
1. Hook extracts ticket ID from commit message
2. Fetches AC list via docket ac list TKT-NNN --format json
3. For each incomplete AC with run:: executes and shows pass/fail
4. For each incomplete AC without run:: prompts "Did you complete: <desc>? [y/N]"
5. Any 'n' or failed run blocks the commit with a clear message and fix suggestion

This is the same logic path as LLM auto-check — just different I/O. Both should call the same underlying library function.

Depends on TKT-131 (docket install), TKT-133 (runnable AC commands).

## Acceptance Criteria
- [x] Pre-commit hook renders incomplete AC items when ticket is referenced in commit message : Extended install hook to call hidden interactive AC checker, implemented __hook-ac-check with runnable AC execution and prose prompts, and DOCKET_SKIP_AC bypass; covered in cmd/ac_test.go and install hook tests.
- [x] Runnable ACs auto-execute and show green/red result inline : Extended install hook to call hidden interactive AC checker, implemented __hook-ac-check with runnable AC execution and prose prompts, and DOCKET_SKIP_AC bypass; covered in cmd/ac_test.go and install hook tests.
- [x] Prose ACs prompt y/n — answering n blocks commit with helpful message and fix suggestion : Extended install hook to call hidden interactive AC checker, implemented __hook-ac-check with runnable AC execution and prose prompts, and DOCKET_SKIP_AC bypass; covered in cmd/ac_test.go and install hook tests.
- [x] All ACs complete → commit proceeds without extra prompts : Extended install hook to call hidden interactive AC checker, implemented __hook-ac-check with runnable AC execution and prose prompts, and DOCKET_SKIP_AC bypass; covered in cmd/ac_test.go and install hook tests.
- [x] DOCKET_SKIP_AC=1 git commit bypasses the AC check as an emergency escape hatch : Extended install hook to call hidden interactive AC checker, implemented __hook-ac-check with runnable AC execution and prose prompts, and DOCKET_SKIP_AC bypass; covered in cmd/ac_test.go and install hook tests.

