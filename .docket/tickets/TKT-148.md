---
id: TKT-148
seq: 148
state: done
priority: 3
labels:
    - feature
    - ecosystem
parent: TKT-173
blocked_by:
    - TKT-147
created_at: "2026-03-12T00:00:00Z"
updated_at: "2026-03-13T21:22:43Z"
created_by: agent:gemini-cli
write_hash: fb95140304117d7acfe6aaf2c752653bb6a2f7b28ac01c0b0991ba22e5a99da8
---

# TKT-148: Implement `docket fix` to repair ticket signatures and teach agents

## Description
Create the "Tutor" command, `docket fix`, which automates the repair of ticket files that have been directly edited by an LLM or human. Instead of just failing, Docket will now intelligently detect the intended change, apply it through the official storage logic (generating a valid `write_hash`), and then explicitly show the LLM the CLI command it *should* have used.

## Acceptance Criteria
- [ ] Implement `docket fix <TKT-ID>`.
- [ ] Command must:
    - Compare the current on-disk (invalid) ticket with the "last known good" version (e.g., from the Git index or a backup).
    - Detect the delta in fields (title, state, priority, etc.).
    - Apply these changes properly using `internal/store` to regenerate a valid `write_hash`.
    - Output a "Tutor Message": "Repairing TKT-148. Do not edit markdown files directly. Use the tools. You should have used: `docket update TKT-148 --field 'value'`."
- [ ] Implement `docket fix --all` to scan and repair all tickets.
- [ ] Command should be the primary suggestion when `docket validate` fails due to a signature mismatch.

## Comments

### 2026-03-13T21:22:33Z — human:Gemini CLI
This ticket remains relevant as the repair and teaching layer for the direct-edit detection flow.

It now sits under the local-trust epic as a supporting UX path:
- TKT-147 detects the mismatch.
- TKT-148 repairs it through official Docket logic.
- Later secure-mode and privileged-transition tickets provide the stronger authority model.

## Handoff
*Last updated: 2026-03-13T00:00:00Z by human:roadmap-maintenance*

**Current state:** Historical roadmap ticket marked done; retained in the graph because later direct-edit protection tickets build on it.

**Decisions made:** Treat `docket fix` as the repair path for invalid `write_hash` mismatches caused by direct ticket edits.

**Files touched:** Historical implementation not reconstructed in this ticket update; see git history for the original change set.

**Remaining work:** No direct implementation remains in this ticket. Follow-up DX work continues in TKT-149 and the newer local-trust roadmap.

**AC status:** Ticket is marked done historically, but the markdown AC list was never fully reconciled when this roadmap item was imported.
