---
id: TKT-215
seq: 215
state: in-progress
priority: 2
labels:
    - feature
parent: TKT-209
created_at: "2026-03-16T14:03:26Z"
updated_at: "2026-03-16T16:12:07Z"
started_at: "2026-03-16T16:09:59Z"
created_by: human:Gemini CLI
write_hash: ca41d8c1087ea472a6f9c2bf20dd0f6f28ba6a235e6622c33f6eb2bd5e92745b
---

# TKT-215: Implement Claude Code adapter for bootstrap, status, and doctor

## Description
Context: Claude integration exists partially but should be unified under the shared adapter contract.

Objective: Implement a Claude Code adapter that aligns with bootstrap, start, and doctor orchestration and keeps behavior idempotent.

Scope:
- Adapter install logic for MCP, skills, and hook surfaces.
- Status and doctor checks with remediation output.
- Non-destructive updates to existing user config.

Out of scope: Claude-only features outside the capabilities contract.

Delivery notes: Adapter output should match shared command UX conventions.

## Acceptance Criteria
- [x] Unit tests validate Claude adapter installs managed artifacts without overwriting unrelated configuration. (run: go test ./internal/adapters -run TestClaudeAdapterInstallMergesMCPConfig|TestClaudeAdapterDoctorRemediationWhenMissing) : Added Claude adapter unit tests verifying managed artifact install and non-clobbering merge behavior for existing MCP config.
- [x] Integration test in simulated Claude workspace verifies bootstrap then doctor PASS. (run: go test ./cmd -run TestClaudeBootstrapCreatesMCPConfigAndDoctorPassesReadiness) : Added simulated Claude workspace integration test verifying bootstrap then doctor PASS across MCP, skills, and hooks checks.
- [x] Validation evidence includes install diff summary and doctor result snapshots linked in ticket comments. (run: DOCKET_HOME=/tmp/docket-home /home/developer/go/bin/docket show TKT-215) : Comment includes install diff summary and doctor snapshot transition evidence.

## Comments

### 2026-03-16T16:12:07Z — human:Gemini CLI
Validation evidence for Claude Code adapter implementation

Install diff and snapshot coverage:
- `internal/adapters/claude_code_test.go` validates non-destructive merge into `.cursor/mcp.json` while preserving unrelated config keys
- bootstrap integration (`cmd/doctor_test.go::TestClaudeBootstrapCreatesMCPConfigAndDoctorPassesReadiness`) asserts `.cursor/mcp.json` artifact creation and doctor readiness transitions

Representative readiness transitions:
- before bootstrap (no MCP config): mcp=FAIL
- after `bootstrap --adapter claude-code`: mcp=PASS, skills=PASS, hooks=PASS

