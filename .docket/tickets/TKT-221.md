---
id: TKT-221
seq: 221
state: in-review
priority: 2
labels:
    - feature
parent: TKT-209
created_at: "2026-03-16T14:03:27Z"
updated_at: "2026-03-16T20:09:12Z"
started_at: "2026-03-16T16:18:25Z"
created_by: human:Gemini CLI
write_hash: 1d92df43c41f4bc6f90cf56136961a771ebae2ec6fe1cac3d12610468fe8839f
---

# TKT-221: Implement minimal lifecycle event set with stable payload schema

## Description
Context: A small stable event vocabulary is needed for observability and future extension without complexity.

Objective: Emit minimal lifecycle events with versioned payloads that adapters and tests can rely on.

Scope:
- Add run.start, phase.end, run.end, and tool.failure events.
- Define stable payload fields and versioning.
- Integrate event emission into relevant command flows.

Out of scope: Large event taxonomy or complex routing.

Delivery notes: Event ordering should be deterministic in integration harness runs.

## Acceptance Criteria
- [x] Unit tests validate payload schema and required fields for each emitted event type. : go test ./internal/lifecycle -run TestValidateEventSchemasAndRequiredFields && go test ./cmd -run TestLifecycleEventsSimulatedRunOrderAndSchemaFixtures
- [x] Integration test executes a simulated run and asserts expected event order and payload presence. : go test -v ./cmd -run TestLifecycleEventsSimulatedRunOrderAndSchemaFixtures (asserts deterministic order: run.start, phase.end, run.end, run.start, tool.failure, phase.end, run.end)
- [x] Validation evidence includes event log fixtures and schema-validation results referenced in comments. : Integration test writes fixture set under .docket/test-fixtures/lifecycle/{events.jsonl,schema-validation.json,event-order.txt} in fake repo and logs paths; schema validation uses lifecycle.ValidateEvent for each emitted event.

## Comments

### 2026-03-16T16:29:18Z — human:Gemini CLI
Implemented minimal lifecycle runtime event stream with stable versioned payload schema and deterministic ordering.

Changes:
- Added internal lifecycle module with event schema validator, JSONL log writer/loader, and run recorder.
- Emitted lifecycle events in `docket start` and `docket __hook-ac-check` flows:
  - run.start
  - phase.end
  - run.end
  - tool.failure (on failing tools)
- Added unit tests for required payload fields across event types.
- Added integration harness test asserting deterministic event order and schema-valid payloads.

Validation evidence:
- go test ./internal/lifecycle -run 'TestValidateEventSchemasAndRequiredFields|TestRecorderWritesAndLoadsLifecycleEvents|TestLogPathUnderRuntimeDir'
- go test -v ./cmd -run TestLifecycleEventsSimulatedRunOrderAndSchemaFixtures
- go test ./...

Fixture evidence (written by integration test in fake repo):
- .docket/test-fixtures/lifecycle/events.jsonl
- .docket/test-fixtures/lifecycle/schema-validation.json
- .docket/test-fixtures/lifecycle/event-order.txt

## Handoff
*Last updated: 2026-03-16T20:09:12Z by human:Codex*

**Current state:** in-review

**Decisions made:** Ticket moved to in-review during triage after validating acceptance criteria completion and linked implementation commits.

**Files touched:** Refer to linked commits and ticket comments for exact file list.

**Remaining work:** Human review and any follow-up fixes requested in review.

**AC status:** 3/3 acceptance criteria are marked done on this ticket.
