---
id: TKT-242
seq: 242
state: done
priority: 2
labels:
    - feature
parent: TKT-239
created_at: "2026-03-16T17:04:59Z"
updated_at: "2026-03-16T18:04:09Z"
created_by: human:Gemini CLI
write_hash: 1d27996279eed812369b11bbb33a05cd8b0b05f0e721d02fce540d7503da9006
---

# TKT-242: Expose screenshot proof metadata in `show`, `context`, and JSON ticket outputs

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-242` before committing. -->

## Description
Context: attached proofs must be discoverable by both humans and LLMs through existing ticket inspection commands.

Objective: surface screenshot proof metadata in `docket show`, `docket context`, and JSON output contracts.

Output requirements: include proof ID, `proof_title`, `note`, timestamps, and image metadata with deterministic ordering and concise human formatting.

Likely paths: `cmd/show.go`, `cmd/context.go`, output tests, and ticket JSON output shapers.

Verify commands: `go test ./cmd -run 'Show|Context|Proof'` and `go test ./...`.

Out of scope: visual thumbnail rendering (handled by UI ticket).

Delivery notes: implement TDD-first and preserve backward compatibility when no proofs exist.

## Acceptance Criteria
- [x] Unit tests (written first) verify proof metadata appears in show/context/json outputs with stable ordering and field names. : Added failing-first tests verifying proof metadata appears in show/context/json outputs with stable field names and deterministic ordering.
- [x] Human output remains concise: proof summaries are visible but do not overwhelm existing ticket sections. : Human output adds concise proof summaries only when proofs exist; context output now includes compact PROOFS line.
- [x] Regression tests confirm consumers remain compatible and outputs are unchanged when a ticket has zero proofs. : Existing no-proof tests still pass and show/json behavior remains unchanged when a ticket has zero proofs.

## Comments

### 2026-03-16T18:03:51Z — human:Gemini CLI
Verification: go test ./cmd -run 'Show|Context|Proof' -count=1 && go test ./...

## Handoff
*Last updated: 2026-03-16T18:55:00Z by human:Codex*

**Current state:** implementation complete and test suite green

**Decisions made:** `show` and `context` now surface proof metadata when present; JSON output includes a `proofs` field while preserving backward-compatible payloads for zero-proof tickets.

**Files touched:** `cmd/show.go`, `cmd/context.go`, `cmd/blame.go`, `cmd/show_test.go`, `cmd/context_test.go`.

**Remaining work:** Query ergonomics and UI rendering tickets will build on this surfaced proof metadata.

**AC status:** All AC completed with evidence; verified using `go test ./cmd -run 'Show|Context|Proof' -count=1` and `go test ./...`.
