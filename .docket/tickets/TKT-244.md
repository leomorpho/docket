---
id: TKT-244
seq: 244
state: done
priority: 2
labels:
    - feature
    - test
parent: TKT-239
created_at: "2026-03-16T17:04:59Z"
updated_at: "2026-03-16T18:33:43Z"
created_by: human:Gemini CLI
---

# TKT-244: Add end-to-end screenshot proof integration suite and quick-path guidance

## Description
Context: we need confidence that agents can execute screenshot proof workflows without manual intervention.

Objective: provide final end-to-end integration validation after storage, CLI, output, remove/gc, integrity events, and scalable UI behavior are in place.

Coverage requirements: fake-repo command traces, attached image fixtures, CLI output verification, UI visibility checks, remove/gc behavior assertions, and lifecycle/ledger event assertions.

Guidance requirements: quick-path docs/help/start output should include canonical proof workflow commands using automation mode.

Likely paths: fake-repo harness tests, help/start guidance modules, proof fixture directories, lifecycle/ledger test fixtures.

Verify commands: targeted integration tests plus `go test ./...`.

Out of scope: external screenshot capture tooling orchestration.

## Acceptance Criteria
- [x] Integration tests (written first) attach sample image proofs and verify visibility in CLI outputs and UI views after full proof pipeline completion. : Added cmd proof pipeline e2e integration plus web UI payload integration tests
- [x] Validation artifacts include reproducible fixture snapshots (proof metadata, command traces, output captures, and event traces) linked in ticket comments. : Fixture/output/event trace artifacts documented in ticket comment and generated by integration harness
- [x] Help/start quick-path guidance includes canonical proof attachment and verification commands with automation-mode hints. : Quick-path guidance now includes proof attach/verify commands with --format json hints in start/capabilities/help-json

## Comments

### 2026-03-16T18:30:37Z — human:Gemini CLI
Validation artifacts and traces for proof pipeline integration are reproducible via tests:
- Go e2e harness fixture snapshots: `cmd/proof_pipeline_integration_test.go` writes
  `.docket/test-fixtures/proof-pipeline/{proof-add.json,show.json,context.json,proof-list.json,proof-remove.json,proof-gc.json,proof-list-after-remove.json,proof-metadata.json,command-trace.txt,proof-events.jsonl}`
- UI visibility integration check: `web/src/routes/page.integration.test.ts` (`includes proof evidence in loaded ticket payloads for UI rendering`)
- Quick-path guidance verification: `cmd/quick_path_integration_test.go`, `cmd/helpjson_test.go`

Verification commands executed:
- `go test ./cmd -run 'TestStartAndCapabilitiesPrioritizeApplyQuickPath|TestHelpJSONCommand|TestProofPipelineEndToEndIntegration'`
- `cd web && pnpm test -- src/routes/page.integration.test.ts`
- `go test ./...`
- `cd web && pnpm test`

## Handoff
*Last updated: 2026-03-16T18:33:30Z by human:Codex*

**Current state:** End-to-end proof pipeline validation and quick-path guidance updates are complete; full suites pass.

**Decisions made:** Added a dedicated fake-repo integration test for proof pipeline command flow + lifecycle traces, and extended quick-path guidance with canonical proof attach/verify JSON commands.

**Files touched:** `cmd/proof_pipeline_integration_test.go`, `cmd/quick_path.go`, `cmd/quick_path_integration_test.go`, `cmd/helpjson.go`, `cmd/helpjson_test.go`, `cmd/start.go`, `cmd/capabilities.go`, `web/src/routes/page.integration.test.ts`.

**Remaining work:** None for this ticket.

**AC status:** All 3 acceptance criteria are complete with evidence and artifact references in ticket comments; verified with targeted tests plus full `go test ./...` and `cd web && pnpm test`.
