---
id: TKT-245
seq: 245
state: done
priority: 2
labels:
    - feature
parent: TKT-239
created_at: "2026-03-16T17:23:50Z"
updated_at: "2026-03-16T17:51:16Z"
created_by: human:Gemini CLI
write_hash: 3808e01bf00863a0f03f10ca420dd3c1e3b9ee1c93ef8d90f3fce2394c0eb844
---

# TKT-245: Implement content-addressed in-repo proof blob storage policy

## Description
Context: proof images should be durable and shareable via git history while minimizing binary duplication and reference breakage.

Objective: store proof image binaries in a content-addressed in-repo layout (hash-keyed) and reference them from ticket proof metadata.

Approach: write files under `.docket/proofs/by-hash/<sha256>.<ext>`, dedupe identical content, and store ticket-level references with stable proof IDs.

Assumptions: local-first tool; demo/mock data usage only; no external hosting in v1.

Likely paths: `internal/proof/*` (new), `internal/store/local/*`, proof metadata serializer, apply/validate helpers.

TDD plan: add failing unit tests for path derivation, dedupe behavior, and deterministic reference output before implementation.

Verify commands: `go test ./internal/... -run 'Proof|Blob'` and `go test ./...`.

## Acceptance Criteria
- [x] Unit tests (written first) verify content-addressed path generation, dedupe on identical bytes, and deterministic blob references. : Added failing-first blob-policy tests validating by-hash path derivation, dedupe behavior, and deterministic references.
- [x] Proof metadata references hash-addressed blobs without duplicate binary writes for identical images. : Refactored proof storage to .docket/proofs/by-hash/<sha>.<ext> and reused existing blobs for identical bytes.
- [x] Regression tests confirm existing proof operations remain stable across repeated add/remove/list cycles. : Added regression add/list cycle test and preserved deterministic ordering and ID behavior while switching to content-addressed blobs.

## Comments

### 2026-03-16T17:50:53Z — human:Gemini CLI
Verification: go test ./internal/... -run 'Proof|Blob' -count=1 && go test ./...

## Handoff
*Last updated: 2026-03-16T18:15:00Z by human:Codex*

**Current state:** implementation complete and test suite green

**Decisions made:** Migrated proof binary storage to content-addressed blobs under `.docket/proofs/by-hash/` keyed by SHA256 plus canonical extension, while keeping ticket-level proof records with stable IDs and deterministic list ordering.

**Files touched:** `internal/proof/repository.go`, `internal/proof/repository_test.go`, `internal/proof/blob_policy_test.go`.

**Remaining work:** Next hardening ticket will tighten MIME allowlist and symlink/path safety constraints.

**AC status:** All AC completed with evidence; verified using `go test ./internal/... -run 'Proof|Blob' -count=1` and `go test ./...`.
