---
id: TKT-247
seq: 247
state: validated
priority: 2
labels:
    - feature
parent: TKT-239
created_at: "2026-03-16T17:23:50Z"
updated_at: "2026-03-16T18:16:56Z"
created_by: human:Gemini CLI
write_hash: ad71a5fdfb4fae7767597be6c40d46a5fb16980629d5ea0f5c4e5fdf02a2c0e8
---

# TKT-247: Define proof remove semantics and add garbage collection for unreferenced blobs

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-247` before committing. -->

## Description
Context: deleting proof references should not accidentally remove shared binaries; binary cleanup still needs an explicit path.

Objective: make `proof remove` detach metadata references only, and add explicit `proof gc` to remove unreferenced blobs.

Approach: maintain proof reference graph, ensure remove is metadata-safe, and implement deterministic mark-and-sweep style GC for blob paths.

Likely paths: proof store/index helpers, `cmd/proof_remove*`, new `cmd/proof_gc*`, integration fixtures.

TDD plan: write failing tests for shared-blob references, remove behavior, and GC reachability before implementation.

Verify commands: `go test ./cmd -run 'Proof(Remove|GC)'` and `go test ./...`.

## Acceptance Criteria
- [x] `docket proof remove` removes ticket proof references without deleting blobs that remain referenced elsewhere. : Added failing-first repository tests validating shared-blob remove safety and deterministic GC behavior for referenced vs unreferenced blobs.
- [x] `docket proof gc` removes only unreferenced blobs and reports deterministic JSON summary counts. : Proof remove now detaches metadata only; shared content-addressed blobs remain until no references exist and GC runs.
- [x] Integration tests (written first) validate shared-blob remove safety and GC cleanup behavior end-to-end. : Added command-level integration test for remove+gc cycle across two tickets sharing one blob and deterministic JSON gc summary outputs.

## Comments

### 2026-03-16T18:16:48Z — human:Gemini CLI
Verification: go test ./internal/proof -run 'Proof.*GC|Proof.*Remove' -count=1 && go test ./cmd -run 'Proof(Remove|GC)' -count=1 && go test ./...

## Handoff
*Last updated: 2026-03-16T19:35:00Z by human:Codex*

**Current state:** implementation complete and test suite green

**Decisions made:** Proof removal now detaches ticket metadata only; blob deletion is handled exclusively by new `docket proof gc`, which scans references and removes only unreferenced content-addressed blobs.

**Files touched:** `internal/proof/repository.go`, `internal/proof/gc_test.go`, `cmd/proof.go`, `cmd/proof_test.go`, `internal/store/local/store.go`.

**Remaining work:** Lifecycle event ticket will now hook into add/remove operations already implemented.

**AC status:** All AC completed with evidence; verified via targeted remove/gc tests and `go test ./...`.
