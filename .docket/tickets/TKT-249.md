---
id: TKT-249
seq: 249
state: done
priority: 2
labels:
    - feature
parent: TKT-239
created_at: "2026-03-16T17:23:50Z"
updated_at: "2026-03-16T18:06:45Z"
created_by: human:Gemini CLI
---

# TKT-249: Add proof query and filter ergonomics for automation and UI consumption

## Description
Context: proof lists need better discoverability for LLM workflows and UI integrations as counts grow.

Objective: extend proof listing with deterministic filters and sorting options.

Approach: support filters such as `--kind`, `--since`, `--actor`, `--limit`, and stable sort defaults (newest-first with deterministic tie-breaks).

Likely paths: `cmd/proof_list*`, proof index/query layer, output contract tests.

TDD plan: add failing tests for filter combinations and deterministic ordering, then implement query layer changes.

Verify commands: `go test ./cmd -run 'Proof.*List|Proof.*Filter'` and `go test ./...`.

## Acceptance Criteria
- [x] Unit tests (written first) verify each filter flag and multi-filter combinations return expected deterministic subsets. : Added failing-first tests for list filters (--kind, --since, --actor, --limit) including combined-filter behavior and deterministic ordering checks.
- [x] JSON output includes stable ordering and pagination/limit behavior suitable for automation. : now supports deterministic newest-first sort with stable tie-breaks plus limit and truncation behavior suitable for automation.
- [x] Regression tests confirm default list output remains backward-compatible when filters are not provided. : Default list contract remains compatible (same ticket_id/proofs JSON shape) when filters are omitted; regression covered in proof command tests.

## Comments

### 2026-03-16T18:06:35Z — human:Gemini CLI
Verification: go test ./cmd -run 'Proof.*List|Proof.*Filter' -count=1 && go test ./cmd -run Proof -count=1 && go test ./...

## Handoff
*Last updated: 2026-03-16T19:05:00Z by human:Codex*

**Current state:** implementation complete and test suite green

**Decisions made:** Added proof list filtering flags (`--kind`, `--since`, `--actor`, `--limit`) with deterministic newest-first ordering and stable tie-breaks for automation-friendly output.

**Files touched:** `cmd/proof.go`, `cmd/proof_test.go`.

**Remaining work:** UI lazy-loading and end-to-end integration tickets can now consume filtered proof list contracts.

**AC status:** All AC completed with evidence; verified using `go test ./cmd -run 'Proof.*List|Proof.*Filter' -count=1`, `go test ./cmd -run Proof -count=1`, and `go test ./...`.
