---
id: TKT-250
seq: 250
state: done
priority: 2
labels:
    - feature
    - ui
    - performance
parent: TKT-239
created_at: "2026-03-16T17:23:50Z"
updated_at: "2026-03-16T18:33:43Z"
created_by: human:Gemini CLI
---

# TKT-250: Add lazy-loading and scalable rendering for screenshot proofs in Ducca UI

## Description
Context: ticket UI must remain responsive when many proofs or large images are attached.

Objective: implement lazy image loading and scalable proof rendering behavior in ticket detail views.

Approach: render metadata first, load previews on demand/viewport, and open full-resolution image via explicit interaction.

Likely paths: Ducca ticket detail components, proof card components, UI state selectors, visual regression fixtures.

TDD plan: add failing component/state tests for lazy-load transitions and fallback states before implementation.

Verify commands: UI unit/integration test suite plus targeted proof rendering tests.

## Acceptance Criteria
- [x] UI tests (written first) verify metadata-first render, deferred image loading, and deterministic proof ordering in large sets. : Validated via web vitest suite and proof rendering tests
- [x] No-proof and load-failure states remain explicit and accessible without breaking ticket layout. : Validated via web vitest suite and proof rendering tests
- [x] Performance-oriented tests/fixtures demonstrate the UI remains usable with multi-proof tickets. : Validated via web vitest suite and proof rendering tests

## Handoff
*Last updated: 2026-03-16T18:33:30Z by human:Codex*

**Current state:** Implementation complete and all tests are green.

**Decisions made:** Chose metadata-first proof cards with explicit preview loading and incremental pagination (`show more proofs`) to keep large-ticket detail views responsive.

**Files touched:** `web/src/lib/components/DetailSheet.svelte`, `web/src/lib/components/DetailSheet.proofs.test.ts`.

**Remaining work:** None for this ticket.

**AC status:** All 3 acceptance criteria are complete with evidence recorded; verified with `go test ./...` and `cd web && pnpm test`.
