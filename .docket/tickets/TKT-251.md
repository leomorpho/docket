---
id: TKT-251
seq: 251
state: archived
priority: 1
labels:
    - bug
    - security
parent: TKT-174
created_at: "2026-03-16T17:23:50Z"
updated_at: "2026-03-23T22:57:18Z"
started_at: "2026-03-23T22:54:38Z"
created_by: human:Gemini CLI
write_hash: f4a757f8c5c60419e3c7d137fbc00e3ac26a551164da526133c77ddca257a03c
---

# TKT-251: Investigate and fix `update --state done` behavior across warning-only and enforced modes

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-251` before committing. -->

## Description
Context: terminal-state gating appears inconsistent because `update --state done` is not consistently applying the expected warning-only or enforced behavior.

Objective: diagnose the policy path and restore deterministic behavior for terminal state transitions under the configured enforcement mode.

Approach: trace the update path, terminal-transition checks, secure session requirements, warning-only behavior, and configuration interactions; then enforce deterministic behavior with regression coverage.

Likely paths: `cmd/update.go`, privileged surface helpers, secure/session modules, transition tests.

TDD plan: write failing regression tests that reproduce missing prompt, missing warning, or incorrect enforcement first, then patch behavior.

Verify commands: `go test ./cmd -run 'Update|Privileged|Secure'` and `go test ./...`.

## Acceptance Criteria
- [x] Regression tests written first reproduce and lock expected terminal-transition behavior for `update --state done` under warning-only and enforced modes. : Added/updated update command regression tests covering agent done transitions in warning-only, enforced, and automation flows (cmd/update_test.go).
- [x] `update --state done` respects the configured enforcement mode consistently in interactive and automation flows, warning when warning-only and blocking only when enforced. : Removed unconditional agent human-only done guard; update done now routes through privileged enforcement gate (cmd/update.go, cmd/privileged_surface.go).
- [x] Failure and warning output remain deterministic and actionable, with guidance on the active enforcement mode and any required secure session or flags. : Verification: go test ./cmd -run 'Update|Privileged|Secure' -count=1 passed; go test ./... executed with one flaky monitor failure on first run and pass on immediate isolated re-run.

## Comments

### 2026-03-23T22:57:14Z — human:Harness User
Aligned done-transition behavior with security enforcement mode: warning-only now warns and proceeds; enforced mode requires privileged surface authorization and secure session. Added regression coverage for agent warning-only, enforced, and automation flows.

## Handoff
**Current state:** in-review

**Decisions made:** removed unconditional LLM done-transition redirect so terminal transitions consistently follow configured enforcement mode; privileged-surface gating remains the single policy path.

**Files touched:** cmd/update.go, cmd/privileged_surface.go, cmd/update_test.go

**Remaining work:** human review of policy semantics and warning copy.

**AC status:** 3/3 complete
