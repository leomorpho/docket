---
id: TKT-251
seq: 251
state: backlog
priority: 1
labels:
    - bug
    - security
parent: TKT-174
blocked_by:
    - TKT-174
created_at: "2026-03-16T17:23:50Z"
updated_at: "2026-03-16T17:23:50Z"
created_by: human:Gemini CLI
write_hash: 047aee62197f7c65d55142e33bd53b31bcefe140367537b76c45154f2306e148
---

# TKT-251: Investigate and fix privileged transition enforcement for `update --state done`

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-251` before committing. -->

## Description
Context: privileged gating appears inconsistent because `update --state done` is not consistently prompting/enforcing expected secure flow.

Objective: diagnose policy path and restore expected behavior for terminal state transitions (including automation semantics).

Approach: trace update path, privileged surface checks, secure session requirements, and configuration interactions; then enforce deterministic behavior with regression coverage.

Likely paths: `cmd/update.go`, privileged surface helpers, secure/session modules, transition tests.

TDD plan: write failing regression tests that reproduce missing prompt/enforcement first, then patch behavior.

Verify commands: `go test ./cmd -run 'Update|Privileged|Secure'` and `go test ./...`.

## Acceptance Criteria
- [ ] Regression tests (written first) reproduce and lock expected privileged enforcement behavior for terminal transitions.
- [ ] `update --state done` enforces configured secure requirements consistently in interactive and automation modes.
- [ ] Failure output remains deterministic and actionable, with guidance on required secure session/flags.

