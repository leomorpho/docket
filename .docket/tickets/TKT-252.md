---
id: TKT-252
seq: 252
state: in-review
priority: 2
labels:
    - workflow
    - automation
    - quality
parent: TKT-174
created_at: "2026-03-16T20:07:23Z"
updated_at: "2026-03-16T20:46:33Z"
started_at: "2026-03-16T20:41:51Z"
created_by: human:Gemini CLI
write_hash: 93157282469ce9fb483feffa15a469212ffc778ebefce773c0292afce83d0494
---

# TKT-252: Auto-transition review-ready tickets out of in-progress

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-252` before committing. -->

## Description
Context:
Several tickets can end up stuck in `in-progress` even after implementation, AC evidence, and linked commits are present. This causes queue ambiguity and makes `docket start` choose poorly.

Objective:
Automatically move tickets that are review-ready from `in-progress` to `in-review` (or designated review semantic state), and prevent stale in-progress tickets from accumulating.

Why:
- Preserves truthful workflow state for humans and LLMs.
- Reduces repeated triage and cleanup passes.
- Keeps `docket start` and board views aligned with real execution status.

Scope:
- Add deterministic review-readiness evaluation in workflow and update paths.
- Apply transition automatically when readiness conditions are met.
- Add guardrails so tickets lacking required evidence remain non-review states with clear diagnostics.
- Respect configurable workflow semantics where review state names may be customized.

Likely paths:
- `cmd/update.go`
- `internal/workflow/service.go`
- `internal/ticket/state.go`
- `internal/hooks/core.go`
- `cmd/update_test.go`
- `cmd/start_test.go`

Out of scope:
- Human-only terminal transitions such as done and archived.
- Changing commit-linkage policy semantics.

TDD plan:
1. Add failing tests for tickets that meet readiness criteria but remain in-progress.
2. Implement readiness evaluation and auto-transition behavior.
3. Add regression tests for cases that must not auto-transition.
4. Validate with CLI integration tests for `start` `update` and automation mode surfaces.

Verify commands:
- `go test ./cmd -run TestUpdateAutoTransitionsReviewReadyTickets`
- `go test ./cmd -run TestUpdateDoesNotAutoTransitionWhenReadinessFails`
- `go test ./cmd -run TestStartAutoPreservesReviewTransitionBehavior`
- `go test ./...`

## Acceptance Criteria
- [x] Unit tests are written first and fail initially for the stale in-progress case and then pass once automatic transition to review semantic state is implemented. : Added TDD-first unit tests (`TestUpdateAutoTransitionsReviewReadyTickets`, `TestUpdateDoesNotAutoTransitionWhenReadinessFails`) and implemented deterministic auto-transition from in-progress to in-review when readiness gates pass.
- [x] Integration coverage proves deterministic automation behavior where review-ready tickets move to review and non-ready tickets remain in non-review states. : Added integration-level behavior coverage in command tests: auto-transition occurs only for review-ready tickets and readiness failures leave tickets in non-review state with deterministic output.
- [x] Diagnostics clearly explain why a ticket did not transition including missing acceptance evidence missing commit linkage or policy gate failures. : Diagnostics now report why auto-transition did not occur including incomplete AC, missing handoff sections, and managed-run/policy gate failures (e.g., commit linkage/worktree gate errors from review transition checks).

## Comments

### 2026-03-16T20:46:23Z — human:Gemini CLI
Completed auto-review transition hygiene implementation.

What changed:
- Added review-readiness evaluator (AC complete + required handoff sections).
- Added deterministic auto-transition hook after non-state `docket update` mutations.
- Added deterministic auto-transition hook after `docket ac complete`.
- Added explicit diagnostics when auto-transition is skipped.
- Preserved existing start transition behavior in `start --auto` test coverage.

Key tests:
- go test ./cmd -run 'TestUpdateAutoTransitionsReviewReadyTickets|TestUpdateDoesNotAutoTransitionWhenReadinessFails|TestStartAutoPreservesReviewTransitionBehavior'
- go test ./cmd
- go test ./...
- cd web && pnpm test
- DOCKET_HOME=/tmp/docket-home-loop-clean3 docket validate

## Handoff
*Last updated: 2026-03-16T20:47:30Z by human:Codex*

**Current state:** in-review

**Decisions made:** Implemented deterministic review-readiness auto-transition logic in update/AC-complete paths with explicit skip diagnostics to avoid stale in-progress tickets while preserving policy gates.

**Files touched:** `cmd/auto_review.go`, `cmd/update.go`, `cmd/ac_complete.go`, `cmd/update_test.go`, `cmd/start_test.go`.

**Remaining work:** Human review and optional refinement of readiness diagnostics phrasing.

**AC status:** 3/3 acceptance criteria completed with test and validation evidence.
