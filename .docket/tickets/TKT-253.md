---
id: TKT-253
seq: 253
state: done
priority: 1
parent: TKT-209
created_at: "2026-03-18T00:12:04Z"
updated_at: "2026-03-18T20:15:12Z"
started_at: "2026-03-18T00:12:05Z"
created_by: human:Gemini CLI
write_hash: f926b6631078825a97c79aaee734b0fde8022a2509da30b8846cd3081bb99d3a
---

# TKT-253: Define Docket skill and hook capability namespaces

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-253` before committing. -->

## Description
Define the product and command model for Docket skills versus hooks so agents and humans can discover the difference quickly. Cover CLI naming, runtime contract changes, and machine-readable discovery surfaces. Implement with tests first and keep the hook surface introspection-only.

## Acceptance Criteria
- [x] The runtime capability contract and command documentation distinguish agent-invoked skills from system-run hooks in a stable, testable schema. : Added explicit docket.skill/docket.hook namespaces with invocation and execution semantics in the runtime contract and capabilities surfaces.
- [x] Automated tests cover the new namespace model and verify that hooks are discoverable without introducing a general hook execution path. : Added tests for contract namespace validation, capability rendering visibility, hidden internal hook commands, and MCP hook.run rejection.

## Comments

### 2026-03-18T00:13:05Z — human:Gemini CLI
Administrative note: this ticket was accidentally advanced to in-progress during batch creation because a shell command example was executed while grooming the ticket. Treat it as unclaimed planning work until someone intentionally starts it.

### 2026-03-18T03:50:54Z — human:codex
Completed namespace-model implementation and tests. Hooks are discoverable via capabilities metadata and docs but remain internal-only for execution.

### 2026-03-18T20:15:07Z — human:Harness User
Session attached: sessions/2026-03-18T201507Z.txt

### 2026-03-18T20:15:12Z — human:Harness User
Session compressed. Handoff updated.

## Handoff
*Last updated: 2026-03-18T20:24:00Z by agent:gpt-5-codex*

**Current state:** The skill and hook capability namespace split is implemented and `TKT-253` remains in `in-review`. Focused command, capability-contract, and MCP discovery tests all pass.

**Decisions made:** Kept hooks introspection-only under the `docket.hook` namespace and agent-facing skills under `docket.skill`, then threaded those namespaces through the canonical capability contract and discovery surfaces so CLI, help, and MCP stay aligned. The implementation favors one contract source of truth rather than per-surface ad hoc strings.

**Files touched:** `internal/capabilities/contract.go` and `internal/capabilities/canonical.go` define the namespace contract. `cmd/hook.go`, `cmd/helpjson.go`, and related command tests expose the distinction through CLI discovery. `internal/mcp/handlers.go` and MCP tests carry the same namespace semantics into machine-facing surfaces.

**Remaining work:** Review and merge the existing implementation. Follow-on tickets can build more workflow on top of these namespaces, but this ticket’s scope is complete.

**AC status:** 2/2 acceptance criteria complete. Re-verified with `./docket ac check TKT-253`, `go test ./cmd/... -run 'TestCapabilities|TestHook|TestSkill'`, and `go test ./internal/capabilities/... ./internal/mcp/...`.
