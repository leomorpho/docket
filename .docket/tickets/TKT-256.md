---
id: TKT-256
seq: 256
state: validated
priority: 1
parent: TKT-209
blocked_by:
    - TKT-280
created_at: "2026-03-18T00:12:05Z"
updated_at: "2026-03-18T20:23:54Z"
started_at: "2026-03-18T00:12:07Z"
created_by: human:Gemini CLI
write_hash: cfcc22bcd8dc61858a289cab6091732682cb8d7f21adba452465586c925578dc
---

# TKT-256: Expose Docket skills through MCP

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-256` before committing. -->

## Description
Extend the MCP surface so agents can discover and invoke Docket skills through the portable tool layer. MCP should consume the same canonical skill metadata used for Agent Skills artifacts and CLI discovery so adapter-specific installs remain optional rather than required.

## Acceptance Criteria
- [x] The MCP server exposes skill discovery and invocation actions using metadata that stays aligned with the canonical Docket skill definitions and Agent Skills artifacts. : Extended MCP dispatch with skill.list, skill.show, and skill.invoke actions backed by canonical runtime skill metadata and invocation templates.
- [x] Unit tests and regression tests cover successful MCP skill discovery and execution and detect drift between MCP skill surfaces and the canonical skill metadata. : Added MCP regression tests for skill list/show/invoke success and missing-ticket failure paths, plus metadata checksum alignment with canonical skill pack.

## Comments

### 2026-03-18T00:13:05Z — human:Gemini CLI
Administrative note: this ticket was accidentally advanced to in-progress during batch creation because a shell command example was executed while grooming the ticket. Treat it as unclaimed planning work until someone intentionally starts it.

### 2026-03-18T04:34:11Z — human:codex
Exposed skills through MCP with discovery and invocation actions that stay synchronized with canonical runtime skill metadata and command templates.

### 2026-03-18T20:23:54Z — human:Harness User
Session attached: sessions/2026-03-18T202354Z.txt

### 2026-03-18T20:23:54Z — human:Harness User
Session compressed. Handoff updated.

## Handoff
*Last updated: 2026-03-18T20:48:00Z by agent:gpt-5-codex*

**Current state:** MCP skill discovery and invocation are implemented and `TKT-256` remains in `in-review`. The focused MCP skill and capability discovery tests pass.

**Decisions made:** Reused the canonical skill metadata and pack-building path for MCP responses instead of introducing a separate MCP-only skill registry. That keeps MCP discovery, CLI discovery, and portable skill artifacts aligned around one source of truth.

**Files touched:** `internal/mcp/handlers.go` adds `skill.list`, `skill.show`, and `skill.invoke` actions backed by the canonical runtime contract and skill pack. `internal/mcp/server_test.go` covers the MCP success and error paths for skill discovery and invocation.

**Remaining work:** Review and merge the existing implementation. Later tickets can broaden packaging and sync across agents, but this ticket’s MCP skill surface is complete.

**AC status:** 2/2 acceptance criteria complete. Re-verified with `./docket ac check TKT-256` and `go test ./internal/mcp/... -run 'TestServeMCP_.*Skill|TestServeMCP_CapabilitiesDiscoveryIncludesSkillMetadata'`.
