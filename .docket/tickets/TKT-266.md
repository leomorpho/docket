---
id: TKT-266
seq: 266
state: in-review
priority: 1
parent: TKT-177
blocked_by:
    - TKT-265
created_at: "2026-03-18T00:12:07Z"
updated_at: "2026-03-18T20:26:11Z"
started_at: "2026-03-18T03:55:36Z"
created_by: human:Gemini CLI
write_hash: a70864d0ec2ef5b59a2c9c2f57af890ff834005433cfc62aee187a93cea13a11
---

# TKT-266: Add an explicit learning replay command for the current ticket

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-266` before committing. -->

## Description
Add a command that replays the most relevant learnings for a given ticket on demand instead of only during docket start. Reuse the current ranking logic where possible and write tests first.

## Acceptance Criteria
- [x] The CLI exposes a learning replay command that returns the top relevant learnings for a given ticket using stable ranking and output rules. : Added learn replay command with stable human/json outputs that reuses buildLearnReplay ranking from docket start.
- [x] Automated tests verify replay behavior for matching and non-matching tickets and keep the command aligned with the same relevance model used by docket start. : Added tests for replay ranking, no-match output, unknown-ticket errors, and alignment with the start-learning relevance model.

## Comments

### 2026-03-18T03:55:36Z — human:codex
Implemented explicit learning replay command via learn replay. The command reuses start-learning ranking logic, supports limit control, and has regression coverage for match/no-match and error paths.

### 2026-03-18T20:26:11Z — human:Harness User
Session attached: sessions/2026-03-18T202611Z.txt

### 2026-03-18T20:26:11Z — human:Harness User
Session compressed. Handoff updated.

## Handoff
*Last updated: 2026-03-18T20:58:00Z by agent:gpt-5-codex*

**Current state:** The explicit learning replay command for the current ticket is implemented and `TKT-266` remains in `in-review`. Focused learning replay tests pass.

**Decisions made:** Reused the same ranking and relevance logic already used by `docket start` so on-demand replay behaves consistently with automatic learning surfacing. This avoids divergent heuristics between entrypoint guidance and explicit replay.

**Files touched:** The learn CLI surfaces under `cmd/` add replay behavior and tests covering ranked replay, empty-state handling, and unknown-ticket behavior.

**Remaining work:** Review and merge the existing implementation.

**AC status:** 2/2 acceptance criteria complete. Re-verified with `./docket ac check TKT-266` and `go test ./cmd/... -run 'TestLearnReplay'`.
