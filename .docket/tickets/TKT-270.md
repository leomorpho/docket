---
id: TKT-270
seq: 270
state: in-review
priority: 2
parent: TKT-177
created_at: "2026-03-18T00:12:07Z"
updated_at: "2026-03-18T02:36:40Z"
started_at: "2026-03-18T02:31:44Z"
created_by: human:Gemini CLI
write_hash: b2869b3b262d46751e2a31f64b0145f716d28066e6ad0f674a3f14f39fc25230
---

# TKT-270: Add a context-optimizer command for compact LLM task briefs

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-270` before committing. -->

## Description
Add a command that assembles a compact, token-conscious task brief from ticket context, related work, learnings, and recent activity. The output should help agents start work quickly without reading every raw surface and should be built test-first.

## Acceptance Criteria
- [x] The CLI exposes a context-optimizer command that produces a concise task brief for a ticket using existing Docket context sources. — evidence: The command remains exposed as `docket context-optimize TKT-NNN`, and the canonical capability contract plus help-json manifest now advertise that exact CLI entrypoint instead of the stale `docket context optimize` form.
- [x] Automated tests verify that the optimized brief includes the required context sources while remaining bounded and predictable in shape. — evidence: `go test ./cmd -run 'TestContextOptimizeCmd_JSONIncludesRequiredSources|TestContextOptimizeCmd_BoundsOutputShape'` passes, and added regression coverage now asserts the canonical contract and help-json manifest keep the correct `context-optimize` command string.

## Comments

### 2026-03-18T02:36:39Z — human:codex
Implemented context-optimize command and bounded brief tests in commit 8d78a4b.

### 2026-03-18T21:41:00Z — agent:gpt-5-codex
Aligned the canonical capability contract and help-json manifest with the shipped `docket context-optimize` command, then added regression tests so agent-facing command metadata cannot drift from the CLI again.

## Handoff
**Current state:** `docket context-optimize` remains implemented and now matches the canonical capability contract and help-json manifest that agents use for discovery.

**Decisions made:** Kept the fix narrowly scoped to the agent-facing metadata surfaces instead of changing the command implementation, because the CLI behavior and bounded brief tests were already correct.

**Files touched:** `internal/capabilities/canonical.go`, `internal/capabilities/contract_test.go`, `cmd/helpjson.go`, `cmd/helpjson_test.go`.

**Remaining work:** Review and merge.

**AC status:** 2/2 acceptance criteria complete. Re-verified with `go test ./cmd -run 'TestContextOptimizeCmd_JSONIncludesRequiredSources|TestContextOptimizeCmd_BoundsOutputShape'`, `go test ./internal/capabilities ./cmd -run 'TestCanonicalContractContextOptimizeCommandMatchesCLI|TestHelpJSONCommand'`, and `go test ./...`.
