---
id: TKT-277
seq: 277
state: done
priority: 1
parent: TKT-228
created_at: "2026-03-18T00:17:09Z"
updated_at: "2026-03-18T02:31:31Z"
started_at: "2026-03-18T02:27:23Z"
created_by: human:Gemini CLI
write_hash: 2672a2f04c8f28ddbb1e7a86f8bde5e277a5c1662644e9a47bcc44aceff6c529
---

# TKT-277: Add shell-safe groomed ticket creation flow for multi-line descriptions and ACs

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-277` before committing. -->

## Description
Improve Docket ticket authoring so agents and humans can create a well-groomed ticket with multi-line description content and acceptance criteria without fighting shell quoting. Prefer a structured input path and explicit guidance that steers users away from brittle inline command construction, and drive the work with tests first.

## Acceptance Criteria
- [x] Docket provides a shell-safe ticket authoring path for multi-line descriptions and multiple acceptance criteria that does not require brittle inline quoting. — evidence: `docket create --authoring-spec <file|->` reads structured YAML/JSON input, populates multiline descriptions and multiple AC entries, and explicitly guides users toward that path instead of brittle inline shell quoting.
- [x] Automated tests cover successful creation of a groomed ticket with structured description and AC input, including failure cases for malformed authoring input. — evidence: `go test ./cmd -run 'TestCreateCmd_AuthoringSpecCreatesMultilineDescriptionAndAC|TestCreateCmd_AuthoringSpecRejectsMalformedInput|TestTicketScaffoldOutputMatchesSchemaAndIsStable|TestTicketApply'` passes.

## Comments

### 2026-03-18T02:31:02Z — human:codex
Implemented shell-safe authoring spec flow in commit 43aac97 (feat/create).

### 2026-03-18T21:49:00Z — agent:gpt-5-codex
Re-verified the structured authoring flow and its malformed-input coverage, then recorded the completion evidence so the ticket state matches the shipped CLI behavior and tests.

## Handoff
**Current state:** The shell-safe ticket authoring path is implemented via `docket create --authoring-spec` and remains covered by focused command tests.

**Decisions made:** Kept this as a ticket-only closeout because the CLI implementation and tests were already in place; the remaining gap was stale acceptance-criteria metadata on the ticket itself.

**Files touched:** `cmd/create.go`, `cmd/create_test.go`, `cmd/scaffold_test.go`, and ticket/apply surfaces were re-verified; this closeout updates only `TKT-277`.

**Remaining work:** Review and merge.

**AC status:** 2/2 acceptance criteria complete. Re-verified with `go test ./cmd -run 'TestCreateCmd_AuthoringSpecCreatesMultilineDescriptionAndAC|TestCreateCmd_AuthoringSpecRejectsMalformedInput|TestTicketScaffoldOutputMatchesSchemaAndIsStable|TestTicketApply'`.
