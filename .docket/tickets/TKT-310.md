---
id: TKT-310
seq: 310
state: draft
priority: 1
labels:
    - epic
    - workflow
    - config
created_at: "2026-03-18T17:46:42Z"
updated_at: "2026-04-01T06:15:27Z"
created_by: agent:gpt-5-codex
write_hash: 6586a7beca678837dd22a947f37bbfdde59c6adb16261b84330212e971c8ec11
---

# TKT-310: [Epic] Make workflow graph config-defined with fixed semantic roles

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-310` before committing. -->

## Description
This epic is now in cleanup mode rather than greenfield implementation. Docket already ships config-backed workflow loading, transition evaluation, startable-state selection, migration helpers, and apply-time validation for custom workflow states. The remaining work is to finish the rollout by eliminating the last hardcoded assumptions, making workflow-source diagnostics explicit, refreshing help/docs/caches, and keeping human- and machine-facing readiness/reporting surfaces aligned with the evaluator.

Scope:
- Keep config-defined workflow graph and state names as the only intended source of truth.
- Preserve fixed semantic roles such as intake, active, review, completed, and archived.
- Finish parity across diagnostics, indexing, docs/help, and compatibility surfaces.

Out of scope:
- Arbitrary user-defined runtime roles with no product semantics.
- Building a fully generic workflow engine for every imaginable lifecycle.

## Acceptance Criteria
- [ ] All child tickets under this epic land in dependency order with deterministic tests and a shared role-driven workflow evaluator or helper layer.
- [ ] The shipped default workflow reproduces current Docket behavior while allowing custom state names, configurable graph transitions, and multiple states per semantic role.
- [ ] Hardcoded workflow state-name assumptions are removed from core runtime paths that should follow config-defined graph and role semantics, while fixed product policy remains explicit in code.

