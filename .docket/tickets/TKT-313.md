---
id: TKT-313
seq: 313
state: in-review
priority: 1
labels:
    - workflow
    - config
parent: TKT-310
created_at: "2026-03-18T17:46:42Z"
updated_at: "2026-03-18T18:08:34Z"
started_at: "2026-03-18T18:03:47Z"
created_by: agent:gpt-5-codex
write_hash: 12c7cfc3482b7c266c52f7fb4df6fee244a72cbc9a0986b054c75a0427c43fd2
---

# TKT-313: Define workflow config v1 schema for semantic roles, per-state behavior, and presentation metadata

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-313` before committing. -->

## Description
Add a versioned workflow config schema that separates semantic workflow behavior from presentation metadata. The schema should support state roles, per-state flags such as open or terminal or startable or reviewable or blocks_dependents, transition rules, and presentation fields like label or column ordering. Implement it from failing tests first so custom workflows can be validated deterministically.

## Acceptance Criteria
- [x] Tests are added or updated first to validate workflow config v1 schema success cases and field-level failure cases for roles, per-state semantics, transitions, and presentation metadata. : Added workflow schema success/failure tests in internal/ticket/config_test.go covering roles, per-state semantics, transitions, and presentation validation before implementing the loader changes.
- [x] Docket can load and validate a versioned workflow config that separates semantic workflow behavior from presentation metadata without relying on hardcoded state-name assumptions. : LoadConfig now accepts a versioned workflow block, derives runtime state config from nested semantics/presentation fields, and validates workflow field paths without relying on state-name literals.
- [x] Go tests pass for the schema and validation changes. : go test ./internal/ticket/... && go test ./...

## Comments

### 2026-03-18T18:07:48Z — human:Harness User
Implemented workflow config v1 loading in internal/ticket/config.go with a nested workflow schema model, flattened runtime state projection, and explicit field-path validation for roles, semantics, transitions, and presentation metadata. Added focused success/failure tests in internal/ticket/config_test.go, then verified with go test ./internal/ticket/... and go test ./....

## Handoff
**Current state:** Implementation complete. Docket can now load a versioned `workflow` block that separates semantic state behavior from presentation metadata while projecting to the existing runtime `states` shape for compatibility.

**Decisions made:** Added `WorkflowConfig` and nested semantic/presentation state structs, validated the explicit workflow block during config load, preserved legacy `states` loading without forcing strict validation on partial historical configs, and derived a normalized in-memory workflow model from both legacy and versioned config inputs.

**Files touched:** `internal/ticket/config.go` and `internal/ticket/config_test.go`.

**Remaining work:** Follow-on tickets should make the versioned workflow config the persisted default, move command/runtime consumers onto evaluator-driven semantics, and remove remaining hardcoded workflow assumptions outside the loader layer.

**AC status:** All AC complete. Verified with `go test ./internal/ticket/...` and `go test ./...`.
