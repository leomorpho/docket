---
id: TKT-314
seq: 314
state: in-review
priority: 1
labels:
    - workflow
    - config
parent: TKT-310
created_at: "2026-03-18T17:46:42Z"
updated_at: "2026-03-23T21:50:00Z"
started_at: "2026-03-23T21:47:48Z"
created_by: agent:gpt-5-codex
write_hash: b6fc92bee7576f1280127d88899d1678c5187199483d671febf1d1c3cec109f0
---

# TKT-314: Ship a default workflow config that matches current Docket behavior

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-314` before committing. -->

## Description
Replace implicit built-in workflow assumptions with a shipped default workflow config that reproduces current state names, transition behavior, and readiness semantics. Write the tests first so the default config becomes the canonical compatibility baseline before downstream refactors switch consumers over.

## Acceptance Criteria
- [x] Tests are added or updated first to prove the shipped default workflow config reproduces current Docket state names, transitions, and workflow semantics. : Added tests first in internal/ticket/config_test.go: TestDefaultConfigUsesShippedWorkflowDefinition and TestMigrateStateNamesUsesShippedWorkflowDefaultsForKnownStates; verified with go test ./internal/ticket -run 'TestDefaultConfigUsesShippedWorkflowDefinition|TestMigrateStateNamesUsesShippedWorkflowDefaultsForKnownStates|TestDefaultConfigStateDefaults' -v.
- [x] Docket ships and loads a default workflow config from configuration rather than depending on duplicated hardcoded workflow constants for the default graph. : Shipped canonical default workflow payload at internal/ticket/default_workflow_v1.json and updated DefaultConfig/migration logic to derive default states from that workflow config payload instead of duplicated hardcoded defaults.
- [x] Go tests pass for the default-config behavior. : go test ./internal/ticket/... passed.

## Handoff
Current state
In review. Default workflow behavior is now sourced from a shipped workflow config payload and projected into runtime state config.

Decisions made
- Added canonical default workflow file: internal/ticket/default_workflow_v1.json (workflow v1 semantics + presentation).
- Refactored DefaultConfig to derive States from the shipped workflow payload instead of a duplicated hardcoded default map.
- Kept legacy migration/backfill behavior but routed known-state defaults through the same canonical workflow-derived defaults.

Files touched
- internal/ticket/default_workflow_v1.json
- internal/ticket/config.go
- internal/ticket/config_test.go

Remaining work
- Reviewer validation and merge.

AC status
- AC1 complete: added default-workflow-source tests before implementation changes.
- AC2 complete: shipped and loaded canonical workflow payload for defaults.
- AC3 complete: ticket package tests pass.

Verification
- go test ./internal/ticket -run "TestDefaultConfigUsesShippedWorkflowDefinition|TestMigrateStateNamesUsesShippedWorkflowDefaultsForKnownStates|TestDefaultConfigStateDefaults" -v
- go test ./internal/ticket/...

Commit
- 595bbee feat(ticket): ship canonical default workflow config payload
  Ticket: TKT-314
