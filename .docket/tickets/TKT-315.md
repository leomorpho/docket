---
id: TKT-315
seq: 315
state: in-review
priority: 1
labels:
    - workflow
    - engine
parent: TKT-310
created_at: "2026-03-18T17:46:42Z"
updated_at: "2026-03-23T21:52:46Z"
started_at: "2026-03-23T21:50:16Z"
created_by: agent:gpt-5-codex
write_hash: 7052daf180bef7629160a32ec71c86ebc0be7072cf82377cae6db476f1f4bd67
---

# TKT-315: Implement a shared workflow evaluator that resolves transitions, roles, startability, and dependency blocking from config

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-315` before committing. -->

## Description
Build one workflow evaluator that consumes workflow config and answers the semantic questions Docket needs: allowed transitions, state roles, startability, reviewability, and whether a ticket in a given state still blocks dependents. Drive it from failing tests first and make the API broad enough to support future approval or AC-based blocking without another redesign.

## Acceptance Criteria
- [x] Tests are added or updated first to cover transition evaluation, role lookup, per-state flags, startability, reviewability, and blocker-state semantics across default and custom workflow shapes. : Added evaluator-first tests in internal/ticket/evaluator_test.go for transitions, role lookup, startability, reviewability, and dependency blocking semantics across default/custom workflows.
- [x] Docket exposes a shared workflow evaluator that derives semantic behavior from config rather than from hardcoded state names or naive blocked_by checks. : Implemented shared ticket.WorkflowEvaluator in internal/ticket/evaluator.go and adopted it in internal/check/rules.go so blocker semantics and role checks are config-driven instead of ad-hoc checks.
- [x] Go tests pass for the workflow evaluator and its fixtures. : go test ./internal/ticket -run TestWorkflowEvaluator -v && go test ./internal/check -v && go test ./internal/...

## Handoff
Current state
In review. Shared workflow evaluation is now centralized in ticket.WorkflowEvaluator and exercised across default/custom workflow shapes.

Decisions made
- Added internal/ticket/WorkflowEvaluator as a config-driven semantic resolver for transitions, roles, startability, reviewability, and dependency blocking.
- Implemented explicit dependency-blocking semantics that treat missing blockers as blocking until healed.
- Updated internal/check rules to consume the evaluator for active-role detection and blocker-state interpretation.

Files touched
- internal/ticket/evaluator.go
- internal/ticket/evaluator_test.go
- internal/check/rules.go

Remaining work
- Reviewer validation and merge.

AC status
- AC1 complete: evaluator tests added first for default and custom workflow semantics.
- AC2 complete: shared evaluator implemented and consumed by workflow-health checks.
- AC3 complete: internal test suites pass.

Verification
- go test ./internal/ticket -run TestWorkflowEvaluator -v
- go test ./internal/check -v
- go test ./internal/workable ./internal/workflow ./internal/ticket/...
- go test ./internal/...

Commit
- f1bfe4b feat(workflow): add shared config-driven workflow evaluator
  Ticket: TKT-315
