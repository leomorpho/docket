---
id: TKT-336
seq: 336
state: in-review
priority: 1
labels:
    - workflow
    - hooks
parent: TKT-312
created_at: "2026-03-18T17:58:29Z"
updated_at: "2026-03-18T20:19:57Z"
started_at: "2026-03-18T18:08:48Z"
created_by: agent:gpt-5-codex
write_hash: 23459d92a85f7ff1682f2ddcb89f1d24048d992b084089d05eac17c2a8042c8c
---

# TKT-336: Fix pre-commit ticket AC checks to read the pending commit message instead of the previous commit

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-336` before committing. -->

## Description
The current pre-commit AC check reads the previous commit message rather than the pending commit, which can fail unrelated commits and rewrite ticket AC evidence during hook execution. Reproduce the bug with failing tests first, then make the hook read the current commit message source deterministically and avoid mutating unrelated tickets during commit-time validation.

## Acceptance Criteria
- [x] Tests are added or updated first to reproduce the current bug where pre-commit AC checks use the previous commit's ticket trailers instead of the pending commit message. : Added cmd/hook_templates_test.go first to prove the checked-in lefthook configs must read the pending COMMIT_EDITMSG file instead of inspecting the previous commit via git log -1.
- [x] Pre-commit ticket AC checks read the pending commit message deterministically and do not mutate unrelated tickets while validating the commit being made. : Updated lefthook.yml and templates/lefthook.yml to read .git/COMMIT_EDITMSG deterministically, extract all Ticket trailers from the pending commit message, and avoid running AC checks against unrelated previous commits.
- [x] Go tests pass for the pre-commit hook fix. : go test ./cmd/... && go test ./...

## Comments

### 2026-03-18T18:10:15Z — human:Harness User
Added a regression test that reads the checked-in lefthook configs and fails if they regress to git log -1. Updated both lefthook.yml variants to read .git/COMMIT_EDITMSG, extract Ticket trailers from the pending commit message, and only run AC checks for the commit currently being created.

### 2026-03-18T20:15:45Z — human:Harness User
Observed on 2026-03-18 during a real commit: pre-commit AC hook validated TKT-260 while committing with trailer Ticket: TKT-253. Reopening to reproduce with tests and fix commit-message source deterministically.

### 2026-03-18T20:19:57Z — human:Harness User
Session attached: sessions/2026-03-18T201957Z.txt

### 2026-03-18T20:19:57Z — human:Harness User
Session compressed. Handoff updated.

## Handoff
*Last updated: 2026-03-18T20:35:00Z by agent:gpt-5-codex*

**Current state:** `TKT-336` is back in `in-review` with a corrected implementation. Trailer-dependent checks no longer run in `pre-commit`; they now run in `commit-msg`, and the real `git commit -m` regression is covered by tests.

**Decisions made:** A real repo reproduction showed that `pre-commit` can see a stale `.git/COMMIT_EDITMSG` during `git commit -m`, so reading that file earlier in the lifecycle is fundamentally unreliable. The fix splits responsibilities: `pre-commit` keeps only checks that do not depend on commit text, while `commit-msg` owns `Ticket:` trailer parsing, done-state blocking, and AC enforcement.

**Files touched:** `cmd/install_shared.go` now writes both `pre-commit` and `commit-msg` hooks and treats both as managed artifacts. `lefthook.yml` and `templates/lefthook.yml` move `docket-ac-check` to `commit-msg`. `cmd/install_upgrade_test.go` adds a real `git commit -m` regression. `cmd/hook_templates_test.go`, `internal/mcp/handlers.go`, `internal/mcp/server_test.go`, and adapter readiness tests were updated for the two-hook model.

**Remaining work:** Review and merge the hook-phase split. Follow-on work can unify the checked-in lefthook config and install-generated shell hooks more aggressively, but this ticket’s bug is resolved.

**AC status:** 3/3 acceptance criteria complete. Re-verified with `./docket ac check TKT-336`, `go test ./cmd/...`, and `go test ./internal/...`.
