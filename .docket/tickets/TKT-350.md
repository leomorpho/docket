---
id: TKT-350
seq: 350
state: archived
priority: 1
labels:
    - security
    - workflow
    - config
parent: TKT-174
created_at: "2026-03-18T19:07:54Z"
updated_at: "2026-03-23T21:55:21Z"
started_at: "2026-03-23T21:52:55Z"
created_by: human:Harness User
write_hash: da659529d277ead4373f9cc24a9fb325299e5e39448cd26f1d1426d9cbe03d5f
---

# TKT-350: Add simple opt-in security enforcement switch with warning-only-by-default behavior

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-350` before committing. -->

## Description
Introduce a simple repo-shared configuration-backed enforcement switch so security-sensitive behavior is warning-only by default and only becomes enforced when a team explicitly opts in. This rollout starts only after the config-backed workflow evaluator foundation is in place so enforcement behavior does not depend on stale hardcoded workflow rules. Keep the model minimal and avoid a larger mode matrix unless real usage proves it is needed.

## Acceptance Criteria
- [x] Failing tests are added first for warning-only-by-default behavior and explicitly enabled enforcement behavior across the security-enforcement configuration surface. : Added failing coverage first for default advisory vs config-enabled enforcement via TestHookACCheckConfigEnforcedFailsDeterministically in cmd/ac_test.go and verified hook AC behavior with focused go test runs.
- [x] Docket supports a simple repo-shared opt-in enforcement switch instead of assuming secure blocking is always active. : Added repo-shared config switch security_enforcement (internal/ticket/config.go) and wired __hook-ac-check enforcement to read config opt-in with env override compatibility (cmd/hook_ac_check.go), defaulting to warning-only behavior.
- [x] Go tests pass for the simplified enforcement configuration behavior. : go test ./cmd -run 'TestHookACCheck(EnforcedFailsDeterministically|ConfigEnforcedFailsDeterministically|NonInteractiveAdvisoryDefault)' -v && go test ./internal/ticket -run 'TestLoadConfigSecurityEnforcementFlag|TestDefaultConfigStateDefaults' -v

## Handoff
Current state
In review. Security-sensitive AC hook enforcement is now warning-only by default and enforceable via a repo-shared opt-in config switch.

Decisions made
- Added a single repo-shared switch `security_enforcement` to config with default false (warning-only).
- Hook AC checker now enforces failures when either env opt-in is set (legacy compatibility) or config `security_enforcement` is true.
- Preserved existing default advisory behavior when the switch is not enabled.

Files touched
- cmd/hook_ac_check.go
- cmd/ac_test.go
- internal/ticket/config.go
- internal/ticket/config_test.go

Remaining work
- Reviewer validation and merge.

AC status
- AC1 complete: tests added for warning-only default and config-enabled enforcement behavior.
- AC2 complete: repo-shared opt-in enforcement switch implemented and consumed by hook enforcement path.
- AC3 complete: targeted test suites for simplified enforcement behavior pass.

Verification
- go test ./cmd -run "TestHookACCheck(EnforcedFailsDeterministically|ConfigEnforcedFailsDeterministically|NonInteractiveAdvisoryDefault)" -v
- go test ./internal/ticket -run "TestLoadConfigSecurityEnforcementFlag|TestDefaultConfigStateDefaults" -v
- go test ./cmd ./internal/ticket/... (cmd package has known unrelated intermittent sqlite busy integration failure)

Commit
- 7f86bf9 feat(security): add repo-shared opt-in hook enforcement switch
  Ticket: TKT-350
