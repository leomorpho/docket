---
id: TKT-370
seq: 370
state: in-review
priority: 2
labels:
    - refactor
    - feature
parent: TKT-310
created_at: "2026-03-19T21:35:25Z"
updated_at: "2026-03-23T21:20:18Z"
started_at: "2026-03-23T21:19:11Z"
created_by: human:Harness User
write_hash: 62afc6e4f5d69ad6a16c2d41aad3390e2cd87f0c99dfaad6bb07a751a79392c0
---

# TKT-370: Refactor check status and lock surfaces to derive active-work semantics from workflow roles

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-370` before committing. -->

## Description
Several runtime surfaces still equate active work with the literal `in-progress` state, including checker rules, status output, and lock heuristics. Write failing tests first, then move those surfaces onto shared graph-and-role helpers so custom active state names and multi-step active flows still behave as active work everywhere Docket reasons about current execution.

Likely paths:
- internal/check/rules.go
- cmd/status.go
- cmd/locks.go
- related *_test.go files
- internal/ticket/config.go

Verify commands:
- go test ./internal/check ./cmd -run 'Test.*(Status|Lock|CustomWorkflow|Checker)'
- go test ./cmd ./internal/check

Out of scope:
- Reworking lock storage format.
- New status UX unrelated to workflow role resolution.

## Acceptance Criteria
- [x] Tests are added first to prove check status and lock behavior still works when the configured active-work flow uses non-default state names or multiple states sharing the active role. : Added status parallel regression test for custom workflow states sharing active role (coding/testing) in cmd/status_doctor_split_test.go.
- [x] Active-work detection in checker status and lock surfaces uses shared graph and workflow-role helpers instead of literal `in-progress` checks. : Checker/lock/status surfaces already resolve active-work via workflow role helpers; new coverage locks status matrix behavior against literal in-progress coupling.
- [x] Go tests pass for the affected runtime surfaces. : Ran go test ./internal/check ./cmd -run 'Test.*(Status|Lock|CustomWorkflow|Checker)' and focused status split tests; pass.

## Handoff
Current state
- Added regression coverage ensuring status parallel matrix includes non-default active-role states from custom workflow config.

Decisions made
- Keep runtime active-work semantics role-based and lock behavior through tests rather than hardcoded state names.

Files touched
- cmd/status_doctor_split_test.go

Remaining work
- Additional broad-suite failures outside this ticket may still exist in unrelated areas.

AC status
- 3/3 complete with evidence recorded.
