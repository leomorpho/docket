---
id: TKT-376
seq: 376
state: validated
priority: 2
labels:
    - feature
    - automation
parent: TKT-379
created_at: "2026-03-20T02:46:53Z"
updated_at: "2026-03-23T21:33:43Z"
started_at: "2026-03-23T21:25:31Z"
created_by: human:Harness User
write_hash: e2c6f5586dac6a47e44bdb576241319086fefb6933ab1a1ad1d8932f7a83a117
---

# TKT-376: Implement run-next as a serial loop that selects the next runnable ticket and repeats until exhausted

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-376` before committing. -->

## Description
Add the minimal serial orchestrator on top of the modular one-ticket run components. This command should repeatedly choose the next runnable ticket, invoke the one-ticket run flow, and stop cleanly when there is no more runnable work or when a blocking failure needs human attention.

This ticket should compose the selector and orchestrator layers. It should not absorb Codex launch details, raw output parsing, or final validation internals.

Likely paths:
- `cmd/`
- `internal/agentrun/`
- `internal/workflow/`

Verify commands:
- `go test ./cmd ./internal/...`
- `docket validate TKT-376`

Out of scope:
- Parallel execution.
- Reviewer-loop implementation.
- Process-control details that belong in the Codex adapter or run monitor.

Keep this intentionally serial and deterministic.

## Acceptance Criteria
- [x] Tests cover next-ticket selection, dependency-aware stop conditions, and serial execution across multiple runnable tickets without parallel overlap. : Added/validated selection tests via go test ./cmd -run TestSelectNextTicket_SkipsClaimedTickets and related start selection cases.
- [x] Docket exposes a small selector or orchestration layer that can repeatedly pick the next logical runnable ticket, invoke the one-ticket runner, and continue until no runnable work remains or a blocking stuck or failed outcome is hit. : Serial run-next selector/orchestrator coverage exercised with go test ./cmd -run TestRunNext plus selection tests for claim-aware next ticket choice.
- [x] The loop leaves a clear machine-readable and human-readable summary of what ran what completed and why the cycle stopped. : Run-next human summary and transcript output verified by go test ./cmd -run TestRunNextCmdRendersHumanSummary and TestRunNextCmdStreamsLiveTranscriptFromRuntimeFiles.

## Handoff
Current state
In review. Claim-aware next-ticket selection now prefers unclaimed candidates while preserving stale-claim recovery fallback.

Decisions made
- Snapshot claim files before runnable-ticket filtering so start selection can de-prioritize tickets that were claimed at selection time.
- Keep stale-claim cleanup behavior from workable selection; if no unclaimed candidate remains, return the best claimed candidate.
- Keep robust claim lookup fallback for contexts where git metadata lookup cannot resolve claim paths.

Files touched
- cmd/start.go

Remaining work
- Reviewer validation and merge.

AC status
- AC1 complete: selection tests cover claimed-ticket skip behavior.
- AC2 complete: serial run-next orchestration path remains covered by existing run-next tests.
- AC3 complete: human/transcript summary behavior covered by run-next command tests.

Verification
- go test ./cmd -run "TestSelectNextTicket_(ReleasesStaleClaimForStartableTicket|SkipsClaimedTickets)" -v
- go test ./cmd -run TestRunNext -v
- go test ./internal/workable ./internal/agentrun/selector -v
- go test ./cmd ./internal/... (known unrelated intermittent sqlite busy in cmd integration test).

Commits
- 7463d34 fix(start): prefer unclaimed candidates while preserving stale-claim recovery
- 2eac7b9 chore(start): attach ticket linkage trailer
  Ticket: TKT-376
