---
id: TKT-381
seq: 381
state: in-review
priority: 1
labels:
    - feature
    - automation
parent: TKT-379
created_at: "2026-03-20T02:47:37Z"
updated_at: "2026-03-23T21:13:18Z"
started_at: "2026-03-23T21:12:12Z"
created_by: human:Harness User
write_hash: 1225bf19c8d774bd0909d09d7ce9ad863738094fafe7f22f6b18cbf621c996a3
---

# TKT-381: Add run monitoring, RESULT parsing, and timeout handling for Codex-managed ticket runs

<!-- docket: Direct edits are allowed. After editing this ticket, run `docket validate TKT-381` before committing. -->

## Description
Build the runtime observer for the single-ticket Codex runner. Docket should tail process output, detect the structured RESULT line, handle timeouts or abnormal exit, and classify the run into a small deterministic set of terminal outcomes.

This ticket should own monitoring and terminal-result parsing only. It should consume output from the agent adapter and emit structured run outcomes without taking over launch, validation, or ticket-state authority.

Likely paths:
- internal/agentrun/
- internal/security/
- cmd/

Verify commands:
- go test ./cmd ./internal/...
- docket validate TKT-381

Out of scope:
- Semantic parsing of the whole transcript.
- Rich planner/reviewer status taxonomies beyond the small terminal state set.
- Final ticket validation and state transitions.

## Acceptance Criteria
- [x] Tests cover successful RESULT parsing plus failure cases including missing result malformed result and no-output timeout handling. : Added monitor regression test for mixed warning inputs and preserved existing RESULT/malformed/timeout parsing coverage in observer_test.go.
- [x] Docket exposes a run monitor that classifies the running Codex process deterministically to done stuck or failed based on structured output and process outcome. : Observer continues deterministic terminal classification from structured RESULT plus process outcomes, with stable warning precedence under concurrent stream ordering.
- [x] The implementation does not require parsing the full conversation as the source of truth; the structured RESULT contract is authoritative. : Structured RESULT parsing remains authoritative; no full transcript semantic parsing is required for terminal run status.

## Handoff
Current state
- Monitor now enforces deterministic warning precedence when both visible runtime warnings and stderr auth warnings appear in a run.

Decisions made
- Authentication rejection warning is treated as higher-priority operational warning than js_repl availability warning.

Files touched
- internal/agentrun/monitor/observer.go
- internal/agentrun/monitor/observer_test.go

Remaining work
- Repository-wide cmd suite still has unrelated pre-existing failure in claimed-ticket selection path.

AC status
- 3/3 complete with evidence recorded.
