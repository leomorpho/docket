This fixture is a stable Docket project for UI hierarchy regression checks.

Manual UI check:
1. Register this directory as a project in the UI.
2. Open ticket `TKT-900`.
3. Verify hierarchy shows `TKT-901` and `TKT-902` as children.
4. Click `TKT-901`, then use `Back` in the sheet to return to `TKT-900`.
